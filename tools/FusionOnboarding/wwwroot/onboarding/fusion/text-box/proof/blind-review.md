# FusionTextBox — Blind Reviewer Verdict

A blind-reviewer agent reviewed the text-box Playwright suite
(`tests/Alis.Reactive.PlaywrightTests/Components/Fusion/TextBox/WhenUsingFusionTextBox.cs`)
against `.claude/memory/bdd-principles.md` and the Nested Vertical Slice contract.
Its prompt explicitly permitted a REJECT verdict. It reviewed the live working tree
(branch `tiny-safe-but-important-refactorings`), opened the page in a real browser on
the sandbox at `http://localhost:5220/Sandbox/Components/TextBox`, exercised every
behavior the suite pins with its own gestures, and re-verified each finding at source
or in the browser rather than trusting the rework agent's report.

## VERDICT: PASS

The reviewer returned the following verdict object verbatim:

```json
{"verdict":"PASS","findings":["REVIEW TARGET: branch tiny-safe-but-important-refactorings @ HEAD 98a1667b6505dec93e3aead44f2d6625028696c3; working tree had the text-box test + sandbox view/model/controller modified (reviewed the live tree incl. uncommitted edits, not a frozen commit). Verified in a real browser at http://localhost:5220/Sandbox/Components/TextBox.","VERDICT PASS (with findings). All 11 tests are behavior-named, independent (each calls its own OpenProfile() -> fresh NavigateToAndWaitForBoot), real-interaction only, and each fails-when-broken with a nameable bug. grep for Ignore|Skip|Thread.Sleep|Task.Delay|ToContainText|try/catch|page.evaluate|Retry|Assert.Pass -> NONE. No plan-JSON / data-reactive-plan assertion anywhere; the one PostData assertion (line 220-225) is the sanctioned framework gather-pipeline exception. AssertNoConsoleErrors (PlaywrightTestBase.cs:197-201) is a real Assert.That(Is.Empty); NavigateToAndWaitForBoot (line 179-185) genuinely waits for boot. The harness is sound.","BROWSER-VERIFIED WITH MY OWN EYES (every behavior the suite pins): search-icon affordance renders inside the name field on load; typing updates 'Directory preview' (Value) live and 'Replacing the name on record' (PreviousValue); blur after a typed edit shows 'Last saved name: Margie / Changed from: Margaret / Edited by you.' (Changed, IsInteracted=true); 'Use legal name' button shows 'Margaret Whitfield / Changed from: Margie / Filled from the resident’s record.' (programmatic SetValue, IsInteracted=false) — the two branches visibly differ; dietary Focus shows 'Note on file when you opened the field: No shellfish' + guidance swap; dietary Blur shows 'Dietary note captured for kitchen staff. / Captured note: ...'; Save -> green panel 'Saved. Margaret Whitfield’s profile is up to date.' No console errors observed.","Rule-3 strength per test is genuine, not pass-hacked. Examples: filling_the_legal_name...not_a_manual_edit (line 97) is the only test distinguishing IsInteracted=false from =true — I confirmed in-browser that the 'Filled from the resident’s record.' vs 'Edited by you.' branches are different outputs, so deleting the IsInteracted routing would flip this assertion and fail it. starting_an_edit_focuses (line 112) / marking_the_edit_done_moves_focus_off (line 125) use real ToBeFocusedAsync / Not.ToBeFocusedAsync — they break if FocusIn()/FocusOut() are no-ops. saving_a_profile_without_a_name_asks_for_one_first (line 174) is the mandatory unhappy path: it asserts the When().NotEmpty() Else branch message and would fail if the empty-name guard let a save through.","FINDING 1 (screenshot-test smell, ranked highest; does NOT invalidate any test): several spans exist only to echo an internal event-payload member back to the page, which a real resident-profile screen would not carry — #name-replacing 'Replacing the name on record' (Index.cshtml:69, PreviousValue), #name-changed-from 'Changed from' (line 75, PreviousValue), #name-edit-source 'Edited by you.'/'Filled from the resident’s record.' (line 76, IsInteracted), #dietary-on-open 'Note on file when you opened the field' (line 131, Focus.Value). These read as diagnostic readouts of FusionTextBox*Args members. CONSEQUENCE: weakens the 'page reads as a product screen' bar. WHY THIS IS A FINDING, NOT A REJECT: the bdd-principles.md file names Grid's Billing slice as THE exemplar, and that exemplar (Grid/Billing.cshtml:304-313) itself carries a font-mono panel of eight 'waiting' echo spans (toolbar-item-id, toolbar-cancel, toolbar-event, tool-status, ...). The text-box view is in fact MORE product-styled than the gold standard (readouts woven into card prose, no font-mono debug block). Rejecting on echo-span purity would contradict the repo's own exemplar and the documented '5 false alarms' caution. Author option: fold PreviousValue/IsInteracted proof into product-plausible copy or accept parity with the exemplar.","FINDING 2 (coverage gap against the 7-behavior contract, ranked second): the suite covers RENDERS, INTERACTS, GATHERS, SUBMITS, plus an unhappy-path guard — but VALIDATES, CONDITIONALLY VALIDATES, and LIVE-CLEARS are absent. The view wires NO FluentValidation (no .Validate<T>(), no .Required() on the InputField, no validator class); the empty-name check is a pipeline When().NotEmpty() condition (Index.cshtml:148/158), not an inline validation error that live-clears on correction. CONSEQUENCE: three of the seven contract behaviors have zero fails-when-broken coverage for this component. This is a journey-design choice (no validator on the profile card) rather than a broken test, so it is a gap to acknowledge/justify, not a rule violation — but per the Coverage Completeness Gate it must be named, not silently omitted.","FINDING 3 (minor, observed-but-uncovered): in the browser the save-confirmation-panel starts hidden and is revealed via RemoveClass('hidden') on success (Index.cshtml:156-157, 161). No test asserts the panel transitions from hidden to visible directly; saving_the_profile_confirms_it_with_the_residents_name (line 190) asserts SaveConfirmation TEXT via ToHaveTextAsync, which auto-waits for visibility and therefore transitively covers the reveal. Acceptable, but a dedicated ToBeVisibleAsync on #save-confirmation-panel would pin the RemoveClass behavior explicitly.","NON-ISSUE checked and cleared: the live Input event leaves PreviousValue one keystroke behind (I saw 'Replacing... Margi' while the field read 'Margie' when typing char-by-char), but the test uses PreferredName.Fill() -> FillAsync (whole-value set in one step), so PreviousValue is the full prior value 'Margaret' — the assertion ToHaveTextAsync('Margaret') at line 69 is correct, not a defect. Nested-Vertical-Slice isolation holds: own model (TextBoxModel), own view/route (/Sandbox/Components/TextBox), own controller, no static mutable state, fresh navigation per test."]}
```

## Summary

- **Verdict:** PASS (with findings). All 11 tests in `WhenUsingFusionTextBox.cs` obey
  the 5 BDD rules and the Nested Vertical Slice contract; each is fails-when-broken
  with a nameable bug, real-interaction only, and independent.
- **Real-browser proof:** the search-icon affordance, the live `Value`/`PreviousValue`
  preview, the committed `Changed` record, the `IsInteracted` false-vs-true branches
  ("Filled from the resident's record." vs "Edited by you."), the dietary Focus/Blur
  reads, the empty-name guard, and the green Save confirmation were each performed by
  the reviewer's own gestures on the sandbox; no console errors observed.
- **Sanctioned exception only:** the single `request.PostData` assertion (the gather
  proof) is the explicitly-allowed framework gather-pipeline exception; no
  `page.evaluate`, no plan-JSON assert, no skip, no weak assert.
- **Non-blocking findings:** (1) a few payload-echo spans read as diagnostic readouts,
  at parity with the documented Grid/Billing exemplar; (2) VALIDATES /
  CONDITIONALLY VALIDATES / LIVE-CLEARS are uncovered because the profile card wires no
  validator (the empty-name check is a pipeline `When().NotEmpty()` guard, not inline
  validation); (3) the hidden→visible reveal of the save-confirmation panel is covered
  transitively by the text assertion rather than a dedicated `ToBeVisibleAsync`. None
  rose to a REJECT.
