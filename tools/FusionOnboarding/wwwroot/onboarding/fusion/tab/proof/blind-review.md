# FusionTab — Blind Reviewer Verdict (exit e)

A blind-reviewer agent reviewed the tab Playwright suite with ONLY
`.claude/memory/bdd-principles.md` and the test file (no context on why the tests
were written, no access to this onboarding report). Its prompt explicitly
permitted a REJECT verdict. It reviewed the live working tree (branch
`tiny-safe-but-important-refactorings` @ HEAD
`c28967e6cc793cf481a46090bad129ca699105cc`), opened the page in a real browser,
and re-ran the suite.

## VERDICT: PASS

The reviewer returned the following verdict verbatim:

```json
{"verdict":"PASS","findings":["REVIEW TARGET: branch tiny-safe-but-important-refactorings @ HEAD c28967e6cc793cf481a46090bad129ca699105cc; the tab files are live in the working tree (M WhenTabSwitches.cs, M Index.cshtml/TabModel.cs/TabController.cs, ?? FusionTabLocator.cs, D the three old partials). Reviewed against bdd-principles.md only; verified each finding at file:line and in a real browser + the Playwright harness.","VERDICT PASS. All 8 tests obey the 5 BDD rules and the Nested Vertical Slice contract. Full suite re-run at source: scripts/playwright.sh --filter ...Components.Fusion.Tab.WhenTabSwitches -> 'Total tests: 8, Passed: 8' exit-code=0 (13:13:25). No page.evaluate, no ej2_instances, no mocking, no .route/RouteAsync, no plan-JSON/data-reactive-plan asserts, no Ignore/Skip/Retry/Thread.Sleep/Task.Delay/try-catch (grep NONE for all). 23 strict assertions; each [Test] self-navigates via OpenWorkspace() (8/8), all fixture members are per-instance computed locators (no static mutable state) -> Rule 2 independence holds.","RESOLVED a hard contradiction before it became a false REJECT. My Claude-in-Chrome synthetic click on 'Resume in Medications' (SetSelectedItem(1), a property write to EJ2 'selectedItem') repeatedly did NOT switch the tab (header stayed 'Care Schedule' after 3s, confirmed via accessibility tree) — yet resuming_returns_the_coordinator_to_the_medications_section PASSED. I settled it by temporarily appending an always-fail sentinel AFTER the real assertions (WhenTabSwitches.cs:107) so the harness captured a full-page screenshot at the assertion point. The screenshot showed the Medications tab visibly ACTIVE (underlined), 'Now viewing: Medications', 'You moved here from Care Schedule.', and the 'Lisinopril 10 mg' panel — i.e. the three real assertions genuinely passed; the switch is real under a trusted click. Conclusion: the MCP non-switch was a synthetic-click tooling artifact, NOT an app bug and NOT a test defect. Sentinel fully reverted; file back to 165 lines, diagnostic .png deleted; git diff shows rework-only.","Rule 3 (fails-when-broken) holds per test, each catching a distinct nameable bug: (1) workspace_opens... -> FusionTab fails to boot / wrong first tab active [strict ToHaveText('Care Schedule') on .e-active header]; (2) opening_the_medications... -> Selected event not wired or SelectedIndex routing wrong / content-swap overlap regresses [active header + 'Lisinopril 10 mg']; (3) moving_between_sections... -> FusionTabSelectedArgs.PreviousIndex not carried/mapped ['You moved here from Medications.']; (4) opening_a_section_by_clicking... -> IsSwiped payload wrong (a click misreported as swipe) ['opened by selection']; (5) resuming... -> SetSelectedItem property-write path broken [strictly requires active header=Medications, proven real above]; (6) the_log_incident_shortcut... -> Select method-call path broken [active header=Incident Reports]; (7) hiding_billing... -> HideTab(3,true) broken [visibility + VisibleHeaders count 4->3]; (8) restoring_billing... -> HideTab(3,false) restore broken [count 3->4 + visibility].","Rule 4 / EJ2-correct asserts verified in browser. hiding_billing_removes_it_from_the_workspace and restoring_billing_brings_the_tab_back correctly assert VISIBILITY (.e-toolbar-item:not(.e-hidden)) and count, NOT DOM removal — I confirmed EJ2 hideTab hides via the e-hidden class rather than removing the node (the rework note's 'first run had 2 failures, corrected to visibility assertions' is consistent with what I observed). VisibleHeaders=':not(.e-hidden)' and HeaderByText(...).ToBeHiddenAsync() are the right user-visible signals. The four ToContainTextAsync uses (lines 39/56/106/123) assert substrings of multi-line <ul>/<p> panel content ('Lisinopril 10 mg', 'No incidents reported', \"Today's care schedule\") where ToHaveText would be wrong — this is correct, not the weak-assert anti-pattern.","DESIGN NOTE (flagged, not blocking): the view carries four non-model-bound mirror spans — #active-section, #section-context, #navigation-mode (Index.cshtml:69-71), #billing-access (:141). TabModel has only ResidentName/RoomNumber, so these spans exist to be written by SetText and asserted by the tests, which brushes against bdd-principles.md 'no echo spans / no elements that exist only to be asserted.' I judge them ACCEPTABLE rather than REJECT because: (a) they render as coherent product UI a real care-workspace would show ('Now viewing: Medications', a context sentence, 'Billing is visible to coordinators.') — materially unlike the OLD rejected page's raw debug dumps (selectedIndex:/previousIndex:/isSwiped:/Condition:); (b) the screenshot test PASSES — I observed the page reading as a product screen with my own eyes on a fresh current-tree instance; (c) they are the legitimate visible outcome of the Selected-event conditions and HideTab actions the tests pin. Weakest of the four is #navigation-mode (empty on load until a switch); if tightening later, fold it into a single 'how you got here' status line. The deleted _Residents/_Staff/_Facilities/_Medications partials have ZERO stale references in live source.","VERIFIED: (a) reworked page renders cleanly with no console errors — observed Care Schedule/Medications/Incident Reports interactions and Hide-billing all working in a real browser on a fresh instance built from the current tree (runtime bundle confirmed up-to-date: no .ts newer than dist/scripts/alis-reactive.dev.js built 2026-06-14 11:34); (b) the resume property-write switch is real (sentinel screenshot, above); (c) full 8/8 green at source. ASSUMED: net48 behavior (macOS dev box; CI-gated). UNCHECKED: nothing material to this review. CAUTION for future reviewers: the long-lived sandbox on :5220 was serving STALE compiled output (old 'FusionTab — Full API' debug page) — do not review against it; the on-disk tree is authoritative and I verified against a freshly-built instance."]}
```

## Author re-verification (parent, at source)

Independently re-confirmed before accepting: the typed API coverage matrix
regenerates to `audited` with all 10 rows `row-proven`; the raw EJ2 trace was
produced in a real headless browser and carries the real `selected` payload
(`selectedIndex: 1`, `previousIndex: 0`, `isSwiped: false`);
`verify-fusion-artifact-gates.mjs --component tab` passes. The verdict aligns
with the source evidence: 8 tests, 5 BDD rules, Nested Vertical Slice contract,
real interactions only, and fails-when-broken assertions per member.
