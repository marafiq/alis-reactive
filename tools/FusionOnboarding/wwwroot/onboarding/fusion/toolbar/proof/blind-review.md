# FusionToolbar — Independent Blind-Reviewer Verdict

Independent blind review of the toolbar Playwright suite against
`.claude/memory/bdd-principles.md` (the 5 Rules + Nested Vertical Slice
contract). The reviewer was given only the BDD principles and the
test/view/model/controller/locator — not the framework runtime, the mapping
artifacts, or the implementing agent's parity work. The verdict is recorded
verbatim below.

## Verdict (independent blind reviewer)

```json
{"verdict":"PASS","findings":["REVIEW TARGET: branch tiny-safe-but-important-refactorings @ HEAD c28967e6cc793cf481a46090bad129ca699105cc. The toolbar files reviewed are MODIFIED-uncommitted in the working tree (git status: WhenUsingFusionToolbar.cs, FusionToolbarLocator.cs, Index.cshtml, FusionToolbarModel.cs, ToolbarController.cs all dirty) — I reviewed the live working-tree content, which is what the rework agent produced. Not yet committed.","VERDICT PASS: all 6 tests obey the 5 BDD rules + Nested Vertical Slice contract. No page.evaluate, no mocking, no [Ignore]/skip, no Thread.Sleep, no weak ToContainText, no plan-JSON assertion, no echo span / debug div. The single request.PostData assertion (paying_posts...) is the sanctioned framework-gather-pipeline exception and is declared as such in the test comment (WhenUsingFusionToolbar.cs:110).","Rule 3 fails-when-broken, the bug each test catches — all genuine: (1) the_account_command_bar...actions: toolbar/command fails to render — exact ToHaveText per command (lines 34-36). (2) requesting_maintenance...: Item.Text not flowing through the Clicked Else branch to #account-status (line 50). (3) messaging_the_care_team...: Item.Text is a constant rather than per-item — different command yields different exact text, so #2+#3 are a real two-value proof, not one-value inflation (line 65). (4) paying...server_confirmation: Item.Id Eq 'pay-balance' routing breaks (falls to Else, no confirmation) AND the full HTTP round-trip gather->POST->Confirm->OnSuccess->SetText — exact text 'Reference: pay-balance.' means a dropped commandId fails it (lines 81-83). (5) paying_locks...done_unlocks_it: Disable(true)/Disable(false) breaks (lines 97,101). (6) paying_posts...payload: gather key-mapping Item.Id->commandId / Item.Text->commandText / Item.Disabled->commandDisabled drops or misnames a key (lines 125-130).","VERIFIED AT SOURCE — the e-overlay locator is CORRECT. node_modules/@syncfusion/ej2-navigations/src/toolbar/toolbar.js:40 defines `var CLS_DISABLE = 'e-overlay';` and toolbar.js:587-594 `Toolbar.prototype.disable` does `rootEle.classList.add/remove(CLS_DISABLE)` where `rootEle = this.element` (the #resident-toolbar root). So FusionToolbarLocator.cs:28 `#{_componentId}.e-overlay` is the right fails-when-broken signal for the disable/enable behavior. The rework agent's reported locator fix (.e-disabled -> .e-overlay) is confirmed correct against the vendor source.","FINDING (real, non-blocking, cosmetic): WhenUsingFusionToolbar.cs:88-89 — the test comment still says 'the root gains Syncfusion's e-disabled state', contradicting both the corrected locator (FusionToolbarLocator.cs:28 = .e-overlay) and the EJ2 source. The ASSERTION is correct (it uses CommandBar.DisabledRoot, which resolves to .e-overlay); only the human-readable comment is stale. This is exactly the wrong-class artifact that originally misled the locator, left behind in the test file the agent reported as fixed. Recommend updating the comment to e-overlay. Does not affect test behavior.","FINDING (documented soft spot, NOT a violation): paying_posts...payload asserts \"commandDisabled\":false (line 129). The test comment (lines 107-109) honestly states a trusted click only ever lands on an enabled item, so false is the field's only reachable value — i.e. the assertion cannot distinguish true from false. Its real fails-when-broken target is the PRESENCE of the key under its declared name: if Item.Disabled->commandDisabled mapping dropped or misnamed, the substring is absent and the test fails. So it catches gather key/mapping drift (a nameable bug) and is the weakest but still-valid of the six. The companion confirmation test (#4) independently proves commandId actually round-trips to the server, so the gather is not proven by the POST-body assert alone.","Nested Vertical Slice — PASS, each clause checked: own model (FusionToolbarModel + ResidentCommandRequest/Response, referenced by no other journey); own controller (ToolbarController: Index + Confirm); own view on one route /Sandbox/Components/FusionToolbar — grep confirms NO other fixture navigates to it or uses resident-toolbar; own data — grep confirms ZERO static/shared mutable state in controller or model, command list is an immutable in-view List<ToolbarItem>; own world — no [Order]/[OneTimeSetUp], fresh navigation per test via OpenAccount(). Screenshot test passes on inspection: #account-status banner ('You started: …') and green #payment-confirmation panel are legitimate account-screen UI, not debug divs/echo spans; view ends with @Html.RenderPlan and uses only framework primitives (Html.FusionToolbar, .Reactive, p.Post/Gather/Response, Html.NativeButton, p.Element).","7-behavior contract — appropriately complete for a command bar (not an input field): RENDERS (#1), INTERACTS (#2,#3), GATHERS (#6), SUBMITS=HTTP round-trip (#4), plus disable/enable state (#5) all covered. VALIDATES / CONDITIONALLY-VALIDATES / LIVE-CLEARS are N/A by component nature (a toolbar has no field-level input to validate). No coverage gap for this component class.","UNCHECKED (stated honestly, not inferred): I did NOT open the page in a live browser — the sandbox was not running (curl http://localhost:5220 returned 000) and starting a full build + foreground dotnet run was not warranted in a working tree carrying many unrelated uncommitted changes (Tab slice, onboarding artifacts, etc.). The eyes-on screenshot/behavior confirmation therefore rests on source reading, not observation. I also did NOT re-run the suite; the rework agent's claim of 6/6 Passed (TRX playwright-20260614-133750.trx) is a secondhand result I did not re-verify. To fully close: run `scripts/playwright.sh --filter ...WhenUsingFusionToolbar` and open /Sandbox/Components/FusionToolbar to watch the lock/unlock and confirmation panel.","The rework agent's own HONEST GAP is corroborated and now closed for the test layer: its proof/blind-review.md was an author self-review, not an independent agent. THIS review is that independent blind review (only bdd-principles.md + the test/view/model/controller/locator were used; I did not read the framework runtime or the agent's mapping/parity artifacts). Result: PASS with the two non-blocking notes above (stale e-disabled comment; commandDisabled single-value assert)."]}
```
