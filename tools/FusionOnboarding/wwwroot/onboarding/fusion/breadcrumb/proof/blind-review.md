# FusionBreadcrumb — Blind Reviewer Verdict

A blind-reviewer agent reviewed the breadcrumb Playwright suite with ONLY
`.claude/memory/bdd-principles.md`, the test file, and the sandbox view — no context
on why the tests were written and no access to this onboarding tree. Its prompt
explicitly permitted a REJECT verdict. It named its review target per the repo rule,
reviewed the live working tree, ran the suite through `scripts/playwright.sh` against
assets built from current source, re-confirmed the outcome at the source TRX, and then
drove the page in a real Chrome before reaching a verdict.

## VERDICT: PASS

> All 7 tests obey the 5 BDD rules and the Nested Vertical Slice contract, and each is
> fails-when-broken (verified by tracing the runtime path AND observing the live
> browser).

## Reviewer verdict (verbatim)

```json
{"verdict":"PASS","findings":["REVIEW TARGET (named per repo rule): branch tiny-safe-but-important-refactorings, HEAD 9ba774ee997c82ae9acb0d9c1cd07eedac8eb69d. Reviewed the live working tree. Test file: tests/Alis.Reactive.PlaywrightTests/Components/Fusion/Breadcrumb/WhenUsingFusionBreadcrumb.cs (the only breadcrumb Playwright test in source); view: Alis.Reactive.SandboxApp/Areas/Sandbox/Views/Components/Fusion/Breadcrumb/Index.cshtml. I was blind to why the tests were written; I verified behavior from the DSL/runtime source and the live browser, not from the rework note.","VERDICT: PASS. All 7 tests obey the 5 BDD rules and the Nested Vertical Slice contract, and each is fails-when-broken (verified by tracing the runtime path AND observing the live browser). No prohibited patterns present: no page.evaluate, no echo spans, no debug divs, no Plan-JSON assert, no skip/[Ignore], no weak asserts (every visible assertion uses ToHaveText, not ToContainText), no Thread.Sleep/Task.Delay. The single request.PostData assertion is the sanctioned framework-gather-pipeline exception (bdd-principles + Playwright CLAUDE.md).","OBSERVED LIVE (eyes, not inferred): ran scripts/playwright.sh --filter ...WhenUsingFusionBreadcrumb against assets built from current source (wrapper accepted them, build 0 warn/0 err) -> 'Total tests: 7, Passed: 7'; re-confirmed at the source TRX playwright-20260614-100957.trx Counters total=7 passed=7 failed=0 error=0. Then drove the page in a real Chrome: clicking the 'Eleanor Hughes' crumb opened a panel showing heading 'Eleanor Hughes', code badge 'RES-214', icon text 'e-icons e-user', summary 'Eleanor Hughes, Room 214, Memory Care since March 2024.', plus a real POST to /CareRecordBreadcrumb/Open returning 200. Clicking 'Back to the resident's overview' (deterministic ref-click + 2s wait) repainted the trail so the current (non-link) crumb moved Care Plan -> Eleanor Hughes. Console trace was clean (0 errors/warnings) and showed the full plan execution: condition eq, set viewing-now, itemClick -> set heading/icon + http POST 200 + set summary/code + classRemove hidden, and button -> set activeItem + call dataBind().","Rule 3 per test (named bug each catches): (1) the_care_record_opens_showing_the_full_trail_to_the_care_plan -> catches the FusionBreadcrumb builder not rendering the trail, or the DomReady ActiveItem()==carePlanUrl condition mis-evaluating (viewing-now text would be the Else branch); asserts exact current-item 'Care Plan' + full sentence. (2) stepping_up_to_the_resident_opens_that_section_of_the_record -> catches ItemClick not firing or args.Item.Text not flowing to open-section-heading (panel stays hidden / heading empty). (3) the_opened_section_is_tagged_with_its_record_icon -> catches args.Item.IconCss not delivered/applied (span starts empty, so empty/wrong fails ToHaveText 'e-icons e-user'). (4) the_opened_section_loads_the_summary_for_that_records_url -> clicks the COMMUNITY crumb; catches Item.Url gather breaking (server SummaryByUrl miss yields 'no summary on file'). (5) the_opened_section_shows_the_record_code_for_that_crumb_id -> clicks the RESIDENT crumb; catches Item.Id gather breaking (CodeById miss yields 'UNKNOWN'). (4) and (5) use different crumbs, proving Url-keyed and Id-keyed resolution independently, not redundantly. (6) opening_a_section_posts_the_clicked_crumb_to_the_server -> catches any of the four FromEvent gather lines dropping (the missing key vanishes from the JSON body). (7) returning_to_the_resident_overview_moves_the_current_crumb -> asserts 'Care Plan' precondition then 'Eleanor Hughes' after the button; catches SetActiveItem or the chained dataBind() repaint breaking.","NOTED (low severity, not a blocker) — the 'disabled':false assertion in opening_a_section_posts_the_clicked_crumb_to_the_server is a CONSTANT, not a discriminator of value correctness. No trail item ever sets Disabled, so the gather can only ever emit false; the test cannot distinguish 'Item.Disabled correctly read as false' from 'this key always emits false'. I verified at the runtime source that it IS discriminating for the gather carrying the field at all: shape-convert.ts applyScalar returns the value unchanged on a failed bool conversion, and gather.ts/request-payload-writer.ts only nulls empty string (not boolean false) — so if the FromEvent(...Disabled...) line were deleted the key would disappear and Does.Contain('\"disabled\":false') would fail. The rework's own note (pattern P025) acknowledges this and justifies it: a disabled Syncfusion crumb sets pointer-events:none and fires no itemClick, so a disabled-click cannot exist, and a Truthy/Else branch would not discriminate either. Accepted as the allowed gather-body exception; recording the residual gap honestly rather than letting 'passes' imply value-level coverage.","Slice isolation confirmed: own model (FusionBreadcrumbModel + OpenCareSectionRequest/Response in .../Models/Components/Fusion/Breadcrumb/FusionBreadcrumbModel.cs), own route/view/controller under the matching concern path, server lookups are immutable static dictionaries (SummaryByUrl, CodeById) with no mutable shared state, and the 7 tests passed running in parallel via the wrapper. Each test calls OpenCareRecord() to navigate a fresh page (Rule 2). Cardinal Rule respected: no framework files are touched by this slice (the test + sandbox view/controller/model are the Arrange; FusionBreadcrumbReactiveExtensions/Events/Item and the runtime are unchanged). Locators use real Syncfusion output verified in the browser: GetByRole(Link, Name) resolves the crumb text, and CurrentItem = [aria-current='page'] is the genuine current-item marker.","Out-of-scope but observed in the tree (NOT part of this test/view, predate this work; flagging for hygiene only): Alis.Reactive.SandboxApp/wwwroot/sf-breadcrumb-probe.html and Alis.Reactive.SandboxApp/Views/Shared/_SandboxBreadcrumb.cshtml are leftover breadcrumb scratch files. They are not referenced by the reviewed slice and do not affect the verdict. The rework's secondhand metrics (parity 16/16, behavioral-coverage 13/13 members, artifact gates) were NOT independently re-verified here — they are outside a blind BDD review's scope (test file + view), so I neither confirm nor relay them as verified."]}
```

## Author re-verification (parent, at source)

Independently re-confirmed against the live tree before accepting this verdict:

- The breadcrumb artifact-gate verifier passes —
  `node .claude/skills/onboard-fusion-component/scripts/verify-fusion-artifact-gates.mjs --component breadcrumb`
  printed `# Fusion artifact gate check passed for breadcrumb`.
- The behavioral-coverage gate (0b) printed
  `[PASS] breadcrumb — 13/13 members mapped, 7 proving test(s)` against TRX
  `playwright-20260614-100957.trx`.
- The test file `tests/Alis.Reactive.PlaywrightTests/Components/Fusion/Breadcrumb/WhenUsingFusionBreadcrumb.cs`
  holds exactly the 7 named tests; the single `request.PostData` assertion is the
  sanctioned framework-gather exception; every visible assertion uses
  `ToHaveTextAsync`; there is no `page.evaluate`, no `[Ignore]`/skip, and each test
  navigates a fresh page through `OpenCareRecord()`.

The verdict aligns with the source evidence.
