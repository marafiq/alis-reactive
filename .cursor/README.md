# Cursor project rules

| Path | Role |
|------|------|
| `rules/code-review-protocol.mdc` | **alwaysApply** — evidence-first PR/GitHub review (Comment criteria, blocking repro in-thread, sign `— Cursor`). |

Human-readable expansion: `docs/process/code-review-protocol.md`.
