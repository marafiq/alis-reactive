// Validation Condition — V3 uses the plan-level Condition type.
// This module is kept for backward compatibility but delegates to conditions.ts.
// V3 validation rules use the same Condition discriminated union as the reaction tree.

export {};
