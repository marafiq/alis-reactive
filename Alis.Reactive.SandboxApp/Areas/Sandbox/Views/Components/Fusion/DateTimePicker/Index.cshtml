@model DateTimePickerModel
@using Alis.Reactive.Native.Extensions
@using Alis.Reactive.Native.Components
@using Alis.Reactive.Fusion.Components
@{
    ViewData["Title"] = "FusionDateTimePicker";

    var plan = Html.ReactivePlan<DateTimePickerModel>();

    // ── Section 1+2: Property Write (SetValue) + Property Read on DomReady ──

    Html.On(plan, t => t.DomReady(p =>
    {
        p.Component<FusionDateTimePicker>(m => m.MedicationTime).SetValue(new DateTime(2026, 6, 15, 14, 30, 0));
        var comp = p.Component<FusionDateTimePicker>(m => m.MedicationTime);
        p.Element("value-echo").SetText(comp.Value());
    }));
}

<native-vstack gap="Lg">
    <div>
        <native-heading level="H1">FusionDateTimePicker — Full API</native-heading>
        <native-text color="Secondary">
            Exercises all supported FusionDateTimePicker API: property writes, property reads,
            events with typed conditions, and component-read conditions.
            Senior living domain: medication schedule times.
        </native-text>
    </div>

    <!-- Section 1: Property Write (SetValue) -->
    <native-card><native-card-body>
        <native-heading level="H2">1. Property Write (SetValue)</native-heading>
        <p class="text-sm text-text-secondary mb-4">
            On DomReady, sets medication time to 2026-06-15 14:30 via structured prop on the Syncfusion ej2 instance.
        </p>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            @{ Html.InputField(plan, m => m.MedicationTime, o => o.Label("Medication Time"))
                .DateTimePicker(b => b
                    .Placeholder("Select medication time")
                    .Reactive(plan, evt => evt.Changed, (args, p) =>
                    {
                        p.Element("change-value").SetText(args, x => x.Value);
                        p.When(args, x => x.Value).NotNull()
                            .Then(t => t.Element("args-condition").SetText("time selected"))
                            .Else(e => e.Element("args-condition").SetText("no time"));
                        var comp = p.Component<FusionDateTimePicker>(m => m.MedicationTime);
                        p.When(comp.Value()).NotNull()
                            .Then(then =>
                            {
                                then.Element("selected-indicator").Show();
                                then.Element("selected-indicator").SetText("medication scheduled");
                            })
                            .Else(else_ =>
                            {
                                else_.Element("selected-indicator").Hide();
                            });
                    })); }
        </div>
    </native-card-body></native-card>

    <!-- Section 2: Property Read — Echo Value -->
    <native-card><native-card-body>
        <native-heading level="H2">2. Property Read (Value as Source)</native-heading>
        <p class="text-sm text-text-secondary mb-4">
            On DomReady, reads the medication time component's <code>value</code> property and displays it in a text element.
        </p>
        <div class="font-mono text-sm">
            <p>Value echo: <span id="value-echo" class="text-text-muted">&mdash;</span></p>
        </div>
    </native-card-body></native-card>

    <!-- Section 3: Events — Changed with typed condition -->
    <native-card><native-card-body>
        <native-heading level="H2">3. Events (Changed + Typed Condition)</native-heading>
        <p class="text-sm text-text-secondary mb-4">
            The medication time field wires a Changed event. Change shows the new value.
            <code>When(args, x =&gt; x.Value).NotNull()</code> tests typed event-args condition.
        </p>
        <div class="font-mono text-sm space-y-1">
            <p>Change value: <span id="change-value" class="text-text-muted">&mdash;</span></p>
            <p>Args condition: <span id="args-condition" class="text-text-muted">&mdash;</span></p>
            <p>Scheduled: <span id="selected-indicator" hidden class="text-emerald-600">&mdash;</span></p>
        </div>
    </native-card-body></native-card>

    <!-- Section 4: Component-Read Condition -->
    <native-card><native-card-body>
        <native-heading level="H2">4. Component Value Condition</native-heading>
        <p class="text-sm text-text-secondary mb-4">
            Button reads medication time via <code>When(comp.Value()).IsEmpty()</code>
            and shows a warning or confirmation.
        </p>
        <div class="mt-3">
            @(Html.NativeButton("check-medication-btn", "Check Medication Time")
                .CssClass("rounded-md bg-accent px-4 py-2 text-sm font-medium text-white hover:bg-accent/90 transition-colors")
                .Reactive(plan, evt => evt.Click, (args, p) =>
                {
                    var comp = p.Component<FusionDateTimePicker>(m => m.MedicationTime);
                    p.When(comp.Value()).IsEmpty()
                        .Then(t => t.Element("medication-warning").SetText("medication time is required"))
                        .Else(e => e.Element("medication-warning").SetText("medication time set"));
                }))
        </div>
        <div class="font-mono text-sm mt-2">
            <p>Warning: <span id="medication-warning" class="text-text-muted">&mdash;</span></p>
        </div>
    </native-card-body></native-card>

    <!-- Plan JSON -->
    <native-card><native-card-body>
        <native-heading level="H2">Plan JSON</native-heading>
        <pre class="rounded-md bg-slate-900 text-emerald-400 p-4 font-mono text-xs overflow-x-auto max-h-96"><code id="plan-json">@Html.Raw(plan.RenderFormatted())</code></pre>
    </native-card-body></native-card>
</native-vstack>

@Html.RenderPlan(plan)
