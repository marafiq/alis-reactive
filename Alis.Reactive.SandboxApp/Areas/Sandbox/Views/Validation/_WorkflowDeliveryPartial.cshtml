@model Alis.Reactive.SandboxApp.Areas.Sandbox.Models.ValidationMergeHarnessModel
@using Alis.Reactive.Native.Extensions
@using Alis.Reactive.Native.Components
@{
    var plan = Html.ResolvePlan<ValidationMergeHarnessModel>();
}

<div class="space-y-3 rounded-md border border-border p-4">
    <p class="font-mono text-sm text-text-muted">Workflow delivery partial</p>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        @{ Html.Field("Instructions", true, m => m.Nested.Delivery!.Instructions, expr =>
            Html.NativeTextBoxFor(plan, expr)
                .CssClass("rounded-md border border-border px-3 py-1.5 text-sm")
                .Placeholder("Ring the bell twice")
        ); }
        @{ Html.Field("Contact Phone", false, m => m.Nested.Delivery!.ContactPhone, expr =>
            Html.NativeTextBoxFor(plan, expr)
                .CssClass("rounded-md border border-border px-3 py-1.5 text-sm")
                .Placeholder("123-456-7890")
        ); }
    </div>
</div>

@Html.RenderPlan(plan)
