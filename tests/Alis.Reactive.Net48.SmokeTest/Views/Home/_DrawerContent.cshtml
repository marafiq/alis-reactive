<div style="padding: 8px;">
    <h4>Drawer Content</h4>
    <p>This content was loaded via <code>Into()</code> on .NET Framework 4.8.</p>
    <p>The same JSON plan contract powers both net48 and net10.</p>
    <p>
        The C# DSL builds descriptors, <code>plan.Render()</code> serializes to JSON,
        and the JS runtime executes &mdash; no manual JavaScript in views.
    </p>
</div>
