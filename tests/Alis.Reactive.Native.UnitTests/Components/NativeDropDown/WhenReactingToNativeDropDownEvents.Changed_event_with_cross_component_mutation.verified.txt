{
  entries: [
    {
      trigger: {
        kind: custom-event,
        event: status-cascade
      },
      reaction: {
        kind: sequential,
        commands: [
          {
            kind: mutate-element,
            target: Status,
            jsEmit: el.value=val,
            value: active
          },
          {
            kind: mutate-element,
            target: Category,
            jsEmit: el.value=val,
            value: A
          },
          {
            kind: mutate-element,
            target: echo,
            jsEmit: el.textContent = val,
            value: Both updated
          }
        ]
      }
    }
  ]
}