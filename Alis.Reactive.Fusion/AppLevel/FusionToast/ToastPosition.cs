namespace Alis.Reactive.Fusion.AppLevel
{
    /// <summary>
    /// Defines the supported toast placement presets.
    /// </summary>
    public enum ToastPosition
    {
        /// <summary>Places the toast in the top-right corner.</summary>
        TopRight,
        /// <summary>Places the toast at the top center.</summary>
        TopCenter,
        /// <summary>Places the toast in the top-left corner.</summary>
        TopLeft,
        /// <summary>Places the toast in the bottom-right corner.</summary>
        BottomRight,
        /// <summary>Places the toast at the bottom center.</summary>
        BottomCenter,
        /// <summary>Places the toast in the bottom-left corner.</summary>
        BottomLeft
    }
}
