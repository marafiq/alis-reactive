---
title: API Reference
description: Complete public API — every method, every overload, every signature. Auto-generated from XML documentation.
sidebar:
  order: 0
---

<!-- AUTO-GENERATED by tools/ApiDocGenerator. Do not edit manually. -->
<!-- Run: npm run build:api-docs -->

Complete public API for Alis.Reactive. Auto-generated from XML documentation comments.
For usage and examples, see the [Grammar Tree](../../csharp-modules/mental-model/#the-grammar-tree) and the [Reactivity](../../csharp-modules/reactivity/triggers-and-reactions/) pages.

## Alis — Reactive

### BrowserElementMembers

The DOM element contract used by `p.Element(...)`.
            This keeps native DOM onboarding explicit without changing the public DSL.

### ClientValidationRuleBinder

Binds client-side validation rules declared by request validation gates
            into the plan's component-level validation rules.


            Each `ValidationJob` names a form and a validation source type. The registered
            `IClientValidationRuleSource` returns the deterministic client rules for
            that source. This binder maps each model field to the component that
            renders it, or to the deterministic component id a partial will render later, and
            attaches the resulting `ComponentValidation` rules to the form's
            `ContainerScope`. Normal validator execution is separate; this binder
            handles only the client-side rules.

### ComponentEventOnboarding

Shared component-event onboarding path for vertical slices.
            The slice selects a typed event and a rendered component id; this helper builds the
            reaction pipeline and wires the event against the plan-registered component.

### ComponentMethod

Describes a JavaScript method on a reactive component.

```csharp
Mapped()
Named()
```

### ComponentObjectTarget

Deterministic target for a component reference in the Reactive Plan.
            The target owns the component id and vendor before any property, method, or event
            contract is declared against the runtime object.

### ComponentProperty<T>

Describes a readable or writable JavaScript property on a reactive component.
            Component onboarding uses these descriptors to declare the Reactive Plan
            component contract that the runtime can read or mutate.

```csharp
Mapped()
Named()
WithShape()
```

### ComponentRef<T>

Authoring handle for a plan-registered component target. Native and Fusion
            extension methods consume it to declare component property sets, method calls,
            and value reads in the Reactive Plan.

### ComponentRegistration

Immutable registration of a component in the Reactive Plan.
            Populated at view render time by each vertical slice's HtmlExtensions.
            Consumed by gather (HTTP serialization) and validation field binding.

```csharp
// Properties
BindingPath { get; }  // Model binding path used as the join key for gather, validation, and payload serialization.
ComponentId { get; }  // Controlled DOM element ID used as the component key in the Reactive Plan.
ComponentType { get; }  // Component kind recorded for duplicate-registration diagnostics and component-specific metadata.
Shape { get; }  // Shape inferred from `typeof(TProp)` at registration time. Flows to plan JSON and is consumed by gather serialization and validation binding.
ValueMember { get; }  // Component-object member read for the registered input value, such as `value` or `checked`.
Vendor { get; }  // Vendor token, such as `native` or `fusion`, written for runtime component resolution.
```

### ExpressionPathHelper

Converts lambda expressions like `x => x.Address.City` into Reactive Plan
            value paths, MVC model-binding names, or MVC element IDs.

```csharp
ToElementId<T>(expression)
ToElementId<T>(expression)
ToEventPath<T>(expression)
ToEventPath<T>(expression)
ToPath<T>(prefix, expression)
ToPath<T>(prefix, expression)
ToPropertyName<T>(expression)
ToPropertyName<T>(expression)
ToResponsePath<T>(expression)
ToResponsePath<T>(expression)
```

### IAppLevelComponent

Marker interface for layout-owned app components with a well-known component id.

```csharp
// Properties
DefaultId { get; }  // Well-known layout element ID used when the DSL references the app-level component without an explicit ID.
```

### IComponent

Marker interface for a component object that can be referenced in a Reactive Plan.

```csharp
// Properties
Vendor { get; }  // Vendor token written into the Reactive Plan for runtime component resolution.
```

### IdGenerator

Generates collision-free HTML element IDs from model type and property expression.

```csharp
For(modelType, propertyPath)
For<T>(expression)
For<T>(expression)
TypeScope(type)
```

### IInputComponent

Marker interface for model-bound input components that expose a readable value member.

```csharp
// Properties
ValueMember { get; }  // JavaScript component-object member read by gather and validation for model-bound inputs.
```

### ModelBoundInputComponentSlot

The deterministic slot where a Razor-rendered input component joins the Reactive Plan.
            Its controlled component id is the join key across DOM rendering, plan registration,
            validation, gather, partial load/unload, and runtime component lookup.

### Plugin

Declares a Reactive Plan plugin: a named runtime object the DSL does not model,
            exposing typed readable properties, value-returning functions, and commands
            with no return value. A plugin is the intentional escape hatch for runtime behavior
            that does not need a first-class deterministic DSL primitive. Subclass it,
            name the plugin in the base constructor, and declare members in the body.

```csharp
// Properties
Name { get; }  // Plugin registration name resolved by the runtime.
```

```csharp
Command()
Command()
Command()
Command()
Function<T>()
Function<T>()
Function<T>()
Function<T>()
Property<T>()
```

### PluginCommand

Descriptor for a plugin command that returns no value. Chain
            `.Arg<T>()` or `.Args(...)` to set the argument contract.

```csharp
Arg<T>()
Args()
```

### PluginFunction<T>

Descriptor for a plugin function that returns a typed value. Chain
            `.Arg<T>()` or `.Args(...)` to set the argument contract.

```csharp
Arg<T>()
Args()
```

### PluginOperation

Base descriptor for a declared plugin function or command.

```csharp
// Properties
Member { get; }  // Declared plugin target name; root functions and commands report `root`.
PluginName { get; }  // Plugin registration name resolved by the runtime.
```

### PluginProperty<T>

Descriptor for a readable Reactive Plan plugin property.

```csharp
// Properties
Member { get; }  // Readable plugin property name declared in the plan contract.
PluginName { get; }  // Plugin registration name resolved by the runtime.
```

### ReactivePlan<T>

Collects reactive behavior for a view: triggers, reactions, and component registrations.
            Renders the collected behavior as generated Reactive Plan JSON.

```csharp
// Properties
IsPartial { get; }  // Whether this plan represents a partial view that merges into a parent plan.
PlanId { get; }  // Unique plan identifier derived from the model type's full name.
```

```csharp
RegisterPlugin(pluginName, configure)
RegisterPlugin(plugin)
RegisterPlugin<T>()
Render()
Render(services)
RenderFormatted()
RenderFormatted(services)
```

### ResponseBody<T>

Represents the typed response body scope available inside HTTP response routes.

```csharp
Read<T>(expression)
```

### TypedEvent<T>

Describes a component event selected by a component `.Reactive(...)` overload.

```csharp
// Properties
Args { get; }  // Typed payload placeholder used to author event-payload reads.
ObjectEvent { get; }  // Component-object event name written into the Reactive Plan.
```

---

## Builders

### DispatchPayloadBuilder<T>

Composes a custom-event dispatch payload from Reactive Plan value sources and literals.
            Each field is set via a typed expression on `TPayload`,
            matching the event-payload contract consumed by typed custom-event triggers.

```csharp
Set(field, value)
Set(field, value)
Set(field, value)
Set<T>(field, source)
```

### ElementBuilder<T>

Builds DOM mutations on a target element: text, HTML, CSS classes, and visibility.

```csharp
AddClass(className)
Hide()
RemoveClass(className)
SetHtml(html)
SetHtml<T>(source, path)
SetHtml<T>(source)
SetText(text)
SetText<T>(source, path)
SetText<T>(source, path)
SetText<T>(source)
Show()
ToggleClass(className)
```

### IReactionEmitter

Narrow interface for emitting reactions into a pipeline.
            Used by vendor-specific extensions (Fusion, Native) and ComponentRef.

```csharp
// Properties
BuildContext { get; }  // Plan build context used for component registration.
```

```csharp
AddStep()
```

### PipelineBuilder<T>

Builds the ordered reactions that execute when a trigger fires: element mutations,
            event dispatches, HTTP calls, component interactions, and conditional logic.

```csharp
// Properties
Alis#Reactive#Builders#IReactionEmitter#BuildContext { get; }
```

```csharp
Alis#Reactive#Builders#IReactionEmitter#AddStep()
Component<T>(expr)
Component<T>(expr)
Component<T>(refId)
Component<T>()
Confirm(message)
Delete(url)
Dispatch(eventName)
Dispatch<T>(eventName, payload)
DispatchWith<T>(eventName, configure)
Element(elementId)
From<T>(source)
From<T>(args, selector)
FromDom(elementId, member)
FromDom<T>(elementId, member)
FromUrl(paramName)
FromUrl<T>(paramName)
Get(url)
Into(elementId)
Parallel(branches)
Plugin(pluginName, member)
Plugin(pluginName)
Plugin(command)
Plugin<T>(pluginName, member)
Plugin<T>(pluginName)
Plugin<T>(function)
Plugin<T>(property)
PluginProperty<T>(pluginName, member)
Post(url)
Post(url, gather)
Put(url, gather)
ValidationErrors(formId)
When<T>(payload, path)
When<T>(responseBody, path)
When<T>(source)
```

### PluginArgumentCollector

Centralizes plugin argument lowering so value reads and commands enforce
            the same argument contract.

### PluginArgumentTypes

Builds an exact plugin argument contract without imposing an arity limit.

```csharp
Arg<T>()
```

### PluginCallBuilder<T>

Collects arguments for a Reactive Plan plugin command. Call `Fire`
            to append the command to the current pipeline.

```csharp
Arg()
Arg()
Arg()
Arg()
Arg()
Arg()
Arg()
Arg<T>(body, path)
Arg<T>(args, path)
Arg<T>(source)
ArgValue<T>()
Fire()
```

### PluginMemberBuilder<T>

Collects arguments for a value-returning plan-registered plugin method or root function.
            Use the builder where a `TypedPluginSource<T>` is expected.

```csharp
Arg()
Arg()
Arg()
Arg()
Arg()
Arg()
Arg()
Arg<T>(body, path)
Arg<T>(args, path)
Arg<T>(source)
ArgValue<T>()
op_Implicit(b)
```

### PluginTypeBuilder

Declares the functions, properties, and commands a Reactive Plan may use on a named plugin.
            For example, `plan.RegisterPlugin("auth", p => p.Method<string>("getToken"))`.
            Use the argument builder when a host plugin member has fixed argument types.

```csharp
Command(name)
Command(name, arguments)
Command()
Command(arguments)
Function<T>()
Function<T>(arguments)
Method<T>(name)
Method<T>(name, arguments)
Property<T>(name)
Void(name)
Void(name, arguments)
Void()
Void(arguments)
```

### TriggerBuilder<T>

Authors trigger-to-reaction behaviors for a Reactive Plan.

```csharp
CustomEvent(eventName, pipeline)
CustomEvent<T>(eventName, pipeline)
DomReady(pipeline)
ServerPush(url, pipeline)
ServerPush(url, eventType, pipeline)
ServerPush<T>(url, eventType, pipeline)
SignalR(hubUrl, methodName, pipeline)
SignalR<T>(hubUrl, methodName, pipeline)
```

---

## Builders — Arrays

### ElementExpressionCompiler

Compiles a captured per-element lambda into the closed plan-node algebra. A predicate
            becomes a SYNC `ConditionGraph` (compare/all/any/not) whose leaves read the
            element scope; reads rooted at the lambda parameter become element reads, everything else
            is evaluated to a literal. Anything outside the supported set throws at plan render time
            (when the Razor view is first requested) — keeping the captured DSL deterministic.

```csharp
CompileProjection()
```

### ReactiveArray<T>

A typed, deferred array transform. Operators capture authoring intent as
            Reactive Plan `array-op` nodes; they do not execute on the server. Deliberately not
            `IEnumerable`/`IQueryable`, so LINQ extension methods
            are not candidates (no collision) and lambdas are captured, not invoked. Per-element
            predicates and selectors read the element scope; chains compose as plan nodes.

```csharp
All()
Any()
Any()
AsSource()
Count()
Count()
Find()
Find<T>()
OrderBy<T>()
OrderByDescending<T>()
Select<T>()
Sum()
Sum()
Sum()
Where()
```

### ReactiveArraySource<T>

An array-op result exposed as a typed source for component data-source binding.

### ReactiveValue<T>

A scalar value produced by an array operation, such as `Count` or `Sum`.
            It is a `TypedSource<T>`, so it can feed DSL members that read typed
            sources, including `SetText`, `When`, and dispatch payloads.
            Gather intake remains limited to component and plugin sources.

---

## Builders — Conditions

### BranchBuilder<T>

Continues an ordered condition branch after a matching `Then` case.

```csharp
Else(pipeline)
ElseIf<T>(payload, path)
ElseIf<T>(responseBody, path)
ElseIf<T>(source)
```

### ConditionSourceBuilder<T>

Authors comparison nodes for a typed value source.

```csharp
ArrayContains()
Between()
Contains()
EndsWith()
Eq()
Eq()
Falsy()
Gt()
Gt()
Gte()
Gte()
In()
IsEmpty()
IsNull()
Lt()
Lt()
Lte()
Lte()
Matches()
MinLength()
NotEmpty()
NotEq()
NotEq()
NotIn()
NotNull()
StartsWith()
Truthy()
```

### ConditionStart<T>

Entry point for nested condition expressions composed inside `And` and `Or` guards.

```csharp
Confirm(message)
When<T>(payload, path)
When<T>(responseBody, path)
When<T>(source)
```

### GuardBuilder<T>

Represents a completed condition expression that can be composed further or attached to a branch.

```csharp
And(inner)
And<T>(payload, path)
And<T>(responseBody, path)
And<T>(source)
Not()
Or(inner)
Or<T>(payload, path)
Or<T>(responseBody, path)
Or<T>(source)
Then(pipeline)
```

### PayloadTypedSource<T>

Reads a typed value from a Reactive Plan payload scope such as event,
            success, error, or dispatch.

### TypedComponentSource<T>

A typed value source produced by a registered component member.

### TypedPluginPropertySource<T>

Represents a readable plan-registered plugin property in conditions, mutations, or gather.

### TypedPluginSource<T>

Represents the value produced by a plan-registered plugin method call in conditions,
            mutations, or gather.

### TypedSource<T>

Preserves the property type through the condition and mutation pipeline
            for compile-time type safety.

### TypedUrlSource<T>

A typed source whose runtime value is read from the browser `window.location` query string.
            Returned by `PipelineBuilder.FromUrl()` and `PipelineBuilder.FromUrl<T>()`.
            Can be used anywhere a `TypedSource<T>` is accepted, including conditions and request inputs.

---

## Builders — Requests

### GatherBuilder<T>

Configures HTTP request input: body fields, headers, and route template
            parameters.

```csharp
FromEvent<T>(args, path, param)
FromUrl(paramName)
FromUrl(paramName, asParam)
FromUrl<T>(paramName)
FromUrl<T>(paramName, asParam)
Header(name, value)
Header<T>(name, source)
Header<T>(name, args, path)
Include()
IncludeAll()
Plugin<T>(source, paramName)
RouteParam(paramName, value)
RouteParam(paramName, value)
RouteParam(paramName, value)
RouteParam<T>(paramName, source)
RouteParam<T>(paramName, args, path)
Static(param, value)
```

### GatherExtensions

Provides component-source overloads for adding request body fields.

```csharp
Include<T>(self, expr)
Include<T>(self, refId, name)
Include<T>(self, source)
Include<T>(self, source, paramName)
```

### HttpRequestBuilder<T>

Configures one HTTP request reaction: endpoint, request input,
            lifecycle reactions, validation, and response routes.

```csharp
AsFormData()
AsJson()
Delete(url)
Finally(pipeline)
Gather(gather)
Get(url)
Post(url)
Put(url)
Response(response)
Validate<T>(formId)
WhileLoading(pipeline)
```

### ParallelBuilder<T>

Configures request branches that start concurrently in the Reactive Plan.

```csharp
OnAllSettled(pipeline)
```

### ResponseBuilder<T>

Configures success routes, error routes, and the optional follow-up
            request for one HTTP request.

```csharp
Chained(request)
OnError(pipeline)
OnError(statusCode, pipeline)
OnError<T>(pipeline)
OnError<T>(statusCode, pipeline)
OnSuccess(pipeline)
OnSuccess<T>(pipeline)
```

---

## InputField

### InputBoundFieldBase<T>

Framework-agnostic base for a model-bound input field.

```csharp
// Properties
Expression { get; }  // Gets the model property expression this field is bound to.
Helper { get; }  // Gets the platform-specific HTML helper for rendering.
Options { get; }  // Gets the label and required options for this field.
Plan { get; }  // Gets the Reactive Plan that owns this field registration.
```

```csharp
Render()
```

### InputFieldBuilder

Writes the shared field wrapper around a registered input component.

```csharp
Begin()
```

### InputFieldOptions

Configures the label and required marker for an input field wrapper.

```csharp
Label(label)
Required()
```

### InputFieldRenderScope

Writes closing HTML tags when disposed. Pure BCL.

---

## Native

### InputBoundField<T>

A model-bound input field returned by
            `Func{<T>`, ready to receive
            a component extension that renders inside the field wrapper.

---

## Native — Extensions

### HtmlExtensions

Razor view extensions for adding behavior to a Reactive Plan.

```csharp
On<T>(html, plan, trigger)
```

### InputFieldExtensions

Razor view extension for starting a model-bound input field.

```csharp
InputField<T>(html, plan, expression)
InputField<T>(html, plan, expression, configure)
```

### PlanExtensions

Razor view extensions for creating, resolving, and rendering Reactive Plans.

```csharp
ReactivePlan<T>()
RenderPlan<T>(html, plan)
ResolvePlan<T>()
```

---

## PlanModel

### AllCondition

Logical AND: all child conditions must be true.

```csharp
// Properties
Kind { get; }  // JSON discriminator for logical AND conditions. Always `"all"`.
Terms { get; }  // Child conditions that must all evaluate to true.
```

### AnyCondition

Logical OR: at least one child condition must be true.

```csharp
// Properties
Kind { get; }  // JSON discriminator for logical OR conditions. Always `"any"`.
Terms { get; }  // Child conditions where at least one must evaluate to true.
```

### AnyResponseStatusMatch

Matches any HTTP response status in the current success or error group.

```csharp
// Properties
Kind { get; }  // JSON discriminator for any-status matches. Always `"any"`.
```

### ArrayExpression

A plan value node that assembles an array from ordered item expressions.

```csharp
// Properties
Items { get; }  // Item expressions evaluated in order when the runtime builds the array value.
Kind { get; }  // Wire discriminator for array value nodes. Always `"array"`.
Shape { get; }  // Output shape declared by the authoring layer, or `None` when unspecified.
```

### ArrayOperationExpression

A deterministic operation over the elements of an array-shaped value.

```csharp
// Properties
ItemShape { get; }  // Element shape expected from the source array.
Kind { get; }  // Wire discriminator for array operation nodes. Always `"array-op"`.
Op { get; }  // Operation name dispatched by the runtime, such as `count` or `filter`.
Predicate { get; }  // Per-element predicate, or null when the operation does not use one.
Projection { get; }  // Per-element projection, or `null` when the operation does not use one.
Shape { get; }  // Output shape produced by the operation.
Source { get; }  // Value expression evaluated before the runtime normalizes the source to an array.
```

### BranchCase

Pairs an explicit guard with a reaction to execute. A default guard is the else case.

```csharp
// Properties
Guard { get; }  // Guard that decides whether this case runs.
Reaction { get; }  // Reaction to execute when the guard matches.
```

### BranchReaction

Evaluates conditions and executes the first matching case.

```csharp
// Properties
Cases { get; }  // Branch cases evaluated in declaration order.
Kind { get; }  // JSON discriminator for branch reactions. Always `"branch"`.
```

### BrowserObject

A plan-registered object target: an id, a vendor, a contract type, a role,
            and, for inputs, a model binding.

```csharp
WithBindingIfAbsent()
```

### BrowserObjectId

Identifies the plan contract key for a DOM element, component, layout object,
            or plugin.

### CallReaction

Calls a method on a component or DOM element.

```csharp
// Properties
Args { get; }  // Method argument expressions in call order; empty when the method takes no arguments.
Kind { get; }  // JSON discriminator for call reactions. Always `"call"`.
Method { get; }  // Method name to call on the target source.
On { get; }  // Runtime source whose method is called.
```

### CompareCondition

Compares two values using a relational operator.

```csharp
// Properties
ItemShape { get; }  // Declared collection item shape for operators such as `contains`; otherwise `None`.
Kind { get; }  // JSON discriminator for comparison conditions. Always `"compare"`.
Left { get; }  // Left-hand value expression evaluated before comparison.
Op { get; }  // Comparison operator: `eq`, `neq`, `gt`, `gte`, etc.
Shape { get; }  // Declared comparison shape, or `None` when not specified.
```

### CompareOp

String constants for all compare-condition operators.
            Prevents typos when building conditions — callers use
            `CompareOperator.Eq` where a domain value object is available.
            The constants remain the JSON tokens shared with generated TypeScript.

### ComponentSource

Identifies a plan-registered component as the value source.

```csharp
// Properties
Component { get; }  // Plan-registered component key used for runtime object lookup.
Kind { get; }  // JSON discriminator for component sources. Always `"component"`.
```

### ComponentValidation

Validation rules and value source for one component within a validation container.

```csharp
// Properties
Component { get; }  // Component ID used for DOM error display and server field matching.
Value { get; }  // Value expression evaluated before client validation runs.
```

### ConditionGraph

Base class for conditional predicates evaluated when the plan executes. Not constructed in application code.

### ConfirmCondition

Prompts the user for confirmation before the reaction proceeds.

```csharp
// Properties
Kind { get; }  // JSON discriminator for async confirmation conditions. Always `"confirm"`.
Message { get; }  // Confirmation message shown at the user-decision boundary.
```

### ContainerRequestValidationTarget

Represents a request that validates a component container before sending.

```csharp
// Properties
Container { get; }  // Component container ID to validate before sending the request.
Kind { get; }  // JSON discriminator for container validation targets. Always `"container"`.
```

### ContractDriftGate

Fails when the committed `plan.ts` disagrees with what
            `PlanContractGenerator` would emit, proving the generated contract is regenerated
            whenever the C# plan node families change.

```csharp
Check()
```

### ContractDriftResult

The outcome of a drift check: whether the on-disk contract matches the generator.

```csharp
// Properties
Diff { get; }  // First divergence; empty when there is no drift (the empty string is the "no divergence" value).
```

### DispatchReaction

Dispatches a `CustomEvent`.

```csharp
// Properties
Event { get; }  // Custom event name to dispatch.
Kind { get; }  // JSON discriminator for custom event dispatch reactions. Always `"dispatch"`.
```

### DomSource

Identifies a DOM element (by id) whose members are read directly via getElementById.

```csharp
// Properties
Element { get; }  // Element ID resolved via `getElementById`.
Kind { get; }  // JSON discriminator for direct DOM sources. Always `"dom"`.
```

### ExactResponseStatusMatch

Matches one exact HTTP response status code.

```csharp
// Properties
Kind { get; }  // JSON discriminator for exact-status matches. Always `"status"`.
Status { get; }  // HTTP status code that selects this route.
```

### FollowUpRequestChain

Represents a request followed by another request after successful completion.

```csharp
// Properties
Kind { get; }  // JSON discriminator for follow-up request chains. Always `"follow-up"`.
Next { get; }  // Request to run after the current request succeeds.
```

### InjectReaction

Injects HTML into a partial slot.

```csharp
// Properties
Kind { get; }  // JSON discriminator for partial-slot injection reactions. Always `"inject"`.
Slot { get; }  // Partial slot that receives injected HTML.
Value { get; }  // Value expression that resolves to the HTML to inject.
```

### LiteralExpression

A plan value node whose value is serialized directly into generated JSON.

```csharp
// Properties
Kind { get; }  // Wire discriminator for literal value nodes. Always `"literal"`.
Shape { get; }  // Output shape declared by the authoring layer, or `None` when unspecified.
Value { get; }  // Serialized literal payload; may be `null` for explicit null values.
```

### MethodValueReadAccess

Access contract for invoking a source member as a method.

```csharp
// Properties
Args { get; }  // Argument value expressions evaluated in call order before invocation.
Kind { get; }  // Wire discriminator for method reads. Always `"method"`.
```

### NoParallelCompletion

Represents a parallel reaction with no completion reaction.

```csharp
// Properties
Kind { get; }  // JSON discriminator for no completion reaction. Always `"none"`.
```

### NoRequestInput

Represents a request with no authored input.

```csharp
// Properties
Kind { get; }  // JSON discriminator for bodiless request input. Always `"none"`.
```

### NoRequestValidationTarget

Represents a request that does not run client validation before sending.

```csharp
// Properties
Kind { get; }  // JSON discriminator for requests without client validation. Always `"none"`.
```

### NotCondition

Logical NOT: inverts a single child condition.

```csharp
// Properties
Kind { get; }  // JSON discriminator for logical NOT conditions. Always `"not"`.
Term { get; }  // Condition whose result is inverted.
```

### ObjectExpression

A plan value node that assembles an object from named field expressions.

```csharp
// Properties
Fields { get; }  // Field map evaluated by name when the runtime builds the object value.
Kind { get; }  // Wire discriminator for object value nodes. Always `"object"`.
Shape { get; }  // Output shape declared by the authoring layer, or `None` when unspecified.
```

### ParallelCompletion

Base class for parallel completion behavior. Not constructed in application code.

```csharp
// Properties
Kind { get; }  // JSON discriminator for parallel completion behavior.
```

### ParallelReaction

Executes a list of reactions concurrently.

```csharp
// Properties
Completion { get; }  // Completion behavior to run after all steps settle.
Kind { get; }  // JSON discriminator for concurrent reaction groups. Always `"parallel"`.
Steps { get; }  // Reactions started concurrently.
```

### PartialPlanScope

Plan scope for a partial plan that can be loaded into a DOM slot.

```csharp
// Properties
Kind { get; }  // JSON discriminator for partial plans. Always `"partial"`.
```

### Path

An ordered sequence of segments for navigating nested properties on a value.

```csharp
// Properties
Segments { get; }  // Ordered path segments used by runtime path traversal.
```

### PathJsonConverter

Serializes Path as a bare JSON array of PathSegments.
            Plan wire format: [{ "kind": "property", "name": "value" }]

### PathSegment

One step in a property navigation path: a property name or array index.

```csharp
// Properties
Kind { get; }  // JSON discriminator for path segments: `property` or `index`.
```

### PayloadContract

Base class for payload typing contracts. Not constructed in application code.

```csharp
// Properties
Kind { get; }  // JSON discriminator for payload typing contracts.
```

### PayloadSource

Identifies an execution payload scope, such as event data or an HTTP response body.

```csharp
// Properties
Kind { get; }  // JSON discriminator for payload sources. Always `"payload"`.
Scope { get; }  // Payload scope wire term, for example event or success.
Type { get; }  // Payload typing contract used when authoring typed value paths.
```

```csharp
Element()
```

### PlanBuildContext

Construction boundary used by the public DSL builders.
            It delegates domain decisions to the plan authoring state and only exposes the
            narrow mutation verbs the DSL needs while authoring a plan.

### PlanContractGenerator

The single source of the TypeScript plan contract (`plan.ts`): one discriminated union per
            polymorphic base, one interface per concrete node carrying its `kind` literal, camelCase members
            matching `PlanSerializer`, and a `LiteralUnion` per token value object's `.Values`.
            The variant set is curated, not reflected: several plan nodes intentionally narrow one C# class into
            many TS variants (for example `CompareCondition` into one interface per operator) so the runtime
            can switch over an exhaustive union — a CLR property reflection cannot express those splits without
            dropping kinds. `ContractDriftGate` is what keeps this contract honest against the C# model.

### PlanDocument

Immutable plan document: the serialized contract between C# authoring and the Reactive Plan runtime.
            Produced by `BuildPlan` once construction is complete.

### PlanJsonWriter

Stateless serialization mechanics shared by the plan model JSON converters. Writes a named
            property by emitting its name and delegating the value to `JsonSerializer`, in the
            same category as `PlanNodeDiscriminator<T>`. Carries no domain branch.

### PlanScope

Base class for plan merge scope. Not constructed in application code.

```csharp
// Properties
Kind { get; }  // JSON discriminator for plan merge scope.
```

### PlanSerializer

The single owner of plan-document to JSON. Emits camelCase property names; node `kind`
            values pass through verbatim. Compact for transport, formatted for debugging.

```csharp
Serialize()
SerializeFormatted()
```

### PluginSource

Identifies a plan-registered plugin object as the value source.

```csharp
// Properties
Kind { get; }  // JSON discriminator for plugin sources. Always `"plugin"`.
Name { get; }  // Plan-registered plugin name used for runtime object lookup.
Type { get; }  // Plugin object contract key declared in the generated plan.
```

### PropertyValueReadAccess

Access contract for reading a source member as a property.

```csharp
// Properties
Kind { get; }  // Wire discriminator for property reads. Always `"property"`.
```

### ReactionGraph

Base class for all executable actions in a Reactive Plan. Not constructed in application code.

### ReadExpression

A plan value node that reads from a value source when the Reactive Plan executes.

```csharp
// Properties
Access { get; }  // Access contract that tells the runtime whether to read a property or invoke a method.
From { get; }  // Source object or payload scope for this read, such as a component, plugin, or event payload.
Kind { get; }  // Wire discriminator for value read nodes. Always `"read"`.
Member { get; }  // Plan member name resolved against the value source.
Path { get; }  // Nested runtime traversal path, or empty for direct member reads.
Shape { get; }  // Output shape declared by the authoring layer, or `None` when unspecified.
```

### RequestInput

Base class for request input strategies. Not constructed in application code.

### RequestPlan

An HTTP request definition in a Reactive Plan.

```csharp
// Properties
Chain { get; }  // Request chain behavior: terminal or followed by another request after success.
Error { get; }  // Response routes evaluated for failed HTTP responses.
Finally { get; }  // Reactions to execute after the request settles, regardless of outcome.
Input { get; }  // Request input strategy; bodiless requests use `NoRequestInput`.
Method { get; }  // HTTP method to send, such as `GET`, `POST`, `PUT`, or `DELETE`.
Success { get; }  // Response routes evaluated for successful HTTP responses.
Url { get; }  // Request URL, which may contain template placeholders such as `{id}`.
Validation { get; }  // Client validation target evaluated before sending this request.
WhileLoading { get; }  // Reactions to execute while the request is loading.
```

### RequestReaction

Sends an HTTP request as defined by the enclosed `RequestPlan`.

```csharp
// Properties
Kind { get; }  // JSON discriminator for HTTP request reactions. Always `"request"`.
Request { get; }  // HTTP request definition to execute in the async lane.
```

### RequestValidationTarget

Base class for request validation targets. Not constructed in application code.

```csharp
// Properties
Kind { get; }  // JSON discriminator for request validation targets.
```

### ResponseRoute

Routes an HTTP response status match to a reaction.

```csharp
// Properties
Match { get; }  // HTTP status match that selects this response route.
Reaction { get; }  // Reaction to execute when the status match succeeds.
```

### ResponseStatusMatch

Base class for HTTP response status matching. Not constructed in application code.

```csharp
// Properties
Kind { get; }  // JSON discriminator for HTTP response status matching.
```

### RootPlanScope

Plan scope for the root view plan.

```csharp
// Properties
Kind { get; }  // JSON discriminator for root view plans. Always `"root"`.
```

### RuntimeObjectSource

Identifies a plan-declared object whose members can be evaluated at runtime.

### SequenceReaction

Executes a list of reactions in declaration order.

```csharp
// Properties
Kind { get; }  // JSON discriminator for ordered reaction sequences. Always `"sequence"`.
Steps { get; }  // Reactions executed in declaration order.
```

### SetReaction

Sets a property value on a component or DOM element.

```csharp
// Properties
Kind { get; }  // JSON discriminator for set reactions. Always `"set"`.
On { get; }  // Runtime source whose property is set.
Property { get; }  // Property name to set on the target source.
Value { get; }  // Value expression assigned to the target property.
```

### SettledParallelCompletion

Runs a reaction after every parallel branch has settled.

```csharp
// Properties
Kind { get; }  // JSON discriminator for settled completion reactions. Always `"on-settled"`.
Reaction { get; }  // Reaction to run after all parallel branches settle.
```

### Shape

Declares the expected type for a value in the Reactive Plan, such as string or number.
            Used across value expressions, conditions, and validation to ensure type consistency.

```csharp
// Properties
IsScalar { get; }  // Returns true if this shape represents a value that can be meaningfully serialized to a single string. Suitable for HTTP headers, route params, and query strings. Scalars: string, number, boolean, date. Nullable wrapping a scalar is also scalar. Non-scalars: array, object, raw, any, none.
Kind { get; }  // JSON shape kind, such as `string`, `array`, `object`, or `nullable`.
```

```csharp
Equals(other)
Equals()
GetHashCode()
op_Equality()
op_Inequality()
```

### ShapeContractCompatibility

Picks the most specific compatible shape from producer and consumer contracts.
            Returns an explicit conflict when no merged shape can satisfy both contracts.

### ShowValidationErrorsReaction

Displays accumulated validation errors in the target container.

```csharp
// Properties
Container { get; }  // Element ID of the validation error container.
Kind { get; }  // JSON discriminator for validation error display reactions. Always `"show-validation-errors"`.
```

### Source

Base class for value source identifiers in a Reactive Plan. Not constructed in application code.

### TerminalRequestChain

Represents a request with no chained follow-up request.

```csharp
// Properties
Kind { get; }  // JSON discriminator for terminal request chains. Always `"terminal"`.
```

### UrlSource

Reads from the browser `window.location` query string.

```csharp
// Properties
Kind { get; }  // JSON discriminator for URL query sources. Always `"url"`.
```

### ValidationJob

A validation source declared on an HTTP request during plan construction, awaiting
            resolution into its form's `ComponentValidation` rules.


            Recorded when a request calls `.Validate<T>(formId)` and resolved once,
            during Reactive Plan rendering, after plan-registered input components have been
            collected. The job carries only the values resolution needs — never a
            `RequestPlan` reference, so it cannot outlive or drift from the request
            instance it was declared on.

```csharp
// Properties
Container { get; }  // The form element id whose components this validation source covers.
RequestUrl { get; }  // The declaring request's URL. Used only for error context.
ValidationSourceType { get; }  // The source type whose metadata declares deterministic client validation rules.
```

### ValueExpression

Base class for all value nodes in a Reactive Plan. Not constructed in application code.

```csharp
ArrayAny()
ArrayFind()
ArraySum()
InvokeElement()
LiteralRaw()
ReadDom()
ReadUrl()
ReadWholeElement()
```

### ValueReadAccess

Base wire contract for how a value read accesses its target member.

```csharp
// Properties
Kind { get; }  // Wire discriminator for the read access shape.
```

---

## Serialization

### PlanNodeDiscriminator<T>

Serializes a polymorphic plan node by writing its concrete runtime type's properties, so the
            node's own `Kind` property becomes the JSON discriminator. The single discriminator
            mechanism for every plan node family; reading is unsupported because plans are write-only.

```csharp
Read()
Write()
```

---

## Validation

### ClientRule

One client validation rule a developer records. Lowers to a plan
            `ValidationRuleNode` at render time.

### ClientValidationCondition<T>

Completed client validation condition that can be composed.

### ClientValidationConditionBuilder<T>

Builds typed field conditions for client validation rules.

### ClientValidationField

Describes a single field's validation rules within a form.
            The rule source declares the field path and shape before render-time binding.

### ClientValidationFieldConditionStart<T>

Starts a client validation condition from a typed model field.

### ClientValidationFieldRuleBuilder<T>

Writes rules for one client validation field.

### ClientValidationFieldToken<T>

Opaque typed reference to a model field used by client validation rules.

### ClientValidationRulesBuilder<T>

Builds one model's deterministic client validation rules.

### FieldCompare

A single field comparison: read field, apply operator, and optionally compare to a right operand.

### FieldCondition

A symbolic condition tree built from client validation field paths.
            Resolved to `ConditionGraph` at render time when
            the component map is available.

### IClientValidationRuleSource

Provides deterministic client validation rules for a validation source type.

### ReactiveClientValidationBuilder

Registers app-level client validation metadata keyed by validation source type.

### ReactiveClientValidationServiceCollectionExtensions

Registers explicit client validation metadata for Reactive Plans.

### RuleName

The single source of the client validation rule names. Projected into the
            TypeScript `ValidationRuleName` union by the plan contract generator.

---

## Fusion

### FusionComponent

Base type for all Syncfusion-backed components.

```csharp
// Properties
Vendor { get; }
```

---

## Fusion — AppLevel

### FusionConfirm

App-level confirm dialog backed by Syncfusion Dialog.
             Singleton per page — one Syncfusion Dialog instance serves all confirm condition evaluations.

             Implements IAppLevelComponent so it can be resolved without an explicit ID:
               p.Component<FusionConfirm>().Show()

### FusionToast

App-level Syncfusion Toast notification component.

```csharp
// Properties
DefaultId { get; }
```

### FusionToastExtensions

Pipeline and layout extensions for the `IHtmlHelper)`.

```csharp
Danger<T>()
FusionToast()
Hide<T>()
Info<T>()
SetContent<T>()
SetTimeout<T>(ms)
SetTitle<T>()
Show<T>()
ShowCloseButton<T>()
ShowProgressBar<T>()
Success<T>()
Warning<T>()
```

---

## Fusion — Components

### Ej2ColumnRules

Translates a field's client rules into the EJ2 FormValidator object shape
            `{ rule: [value, message] }`. The validator rule names are already EJ2-shaped
            (`required`, `minLength`, `range`, ...), so each maps by name.

### FusionAccordion

Represents a Syncfusion Accordion container component.
            Exposes panel mutation methods and item events; it is not an input component.

### FusionAccordionBuilder<T>

Wraps rendered Syncfusion accordion markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionAccordionEvents

Typed event descriptors for the `FusionAccordion` component.

```csharp
// Properties
Expanded { get; }  // Fires after a panel expands or collapses.
```

### FusionAccordionExpandedArgs

Payload for FusionAccordion.Expanded.
            Fires after a panel expands or collapses.
            Properties are typed markers for expression-based condition sources:
              p.When(args, x => x.IsExpanded).Truthy()
            ExpressionPathHelper resolves x => x.IsExpanded to "evt.isExpanded".

```csharp
// Properties
Index { get; }  // The zero-based index of the panel that was expanded/collapsed.
IsExpanded { get; }  // True if the panel was expanded, false if collapsed.
```

### FusionAccordionExtensions

Mutation extensions for `FusionAccordion` in a reactive pipeline.

```csharp
EnableItem<T>()
ExpandItem<T>()
```

### FusionAccordionHtmlExtensions

Creates typed `FusionAccordionBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionAccordion<T>(html, plan, elementId, build)
```

### FusionAccordionReactiveExtensions

Wires `FusionAccordion` events into the Reactive Plan.

```csharp
Reactive<T>(builder, eventSelector, pipeline)
```

### FusionAIAssistView

Syncfusion EJ2 AIAssistView. Non-input component; reactive behavior is exposed
            through typed methods, state reads, and events.

### FusionAIAssistViewBuilder<T>

Carries rendered Syncfusion AIAssistView markup and the Reactive Plan for event wiring.

### FusionAIAssistViewEvents

Typed AIAssistView events available to Reactive Plans.

### FusionAIAssistViewExtensions

Runtime behavior for Syncfusion AIAssistView. Initial render options remain on
            Syncfusion's AIAssistViewBuilder.

### FusionAIAssistViewHtmlExtensions

Factory extension for rendering Syncfusion AIAssistView with reactive event support.
            Static component configuration stays on Syncfusion's MVC builder.

### FusionAIAssistViewPromptChangedArgs

Payload for AIAssistView promptChanged.

### FusionAIAssistViewPromptRequestArgs

Payload for AIAssistView promptRequest.

### FusionAIAssistViewReactiveExtensions

Wires `FusionAIAssistView` events into the Reactive Plan.

### FusionAIAssistViewStopRespondingClickArgs

Payload for AIAssistView stopRespondingClick.

### FusionAutoComplete

A FusionAutoComplete for typing and filtering suggestions from a data source.

```csharp
// Properties
ValueMember { get; }
```

### FusionAutoCompleteChangeArgs

Event payload delivered when a `FusionAutoComplete` selection changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // Selected value.
```

### FusionAutoCompleteEvents

Typed event descriptors for the `FusionAutoComplete` component.

```csharp
// Properties
Changed { get; }  // Fires when the selected value changes.
Filtering { get; }  // Fires when the user types to filter.
```

### FusionAutoCompleteExtensions

Reactive pipeline extensions for reading and mutating `FusionAutoComplete`.

```csharp
DataBind<T>()
Disable<T>()
Enable<T>()
FocusIn<T>()
FocusOut<T>()
HidePopup<T>()
SetDataSource<T>()
SetDataSource<T>()
SetDataSource<T>()
SetText<T>()
SetValue<T>(value)
ShowPopup<T>()
Value<T>()
```

### FusionAutoCompleteFilteringArgs

Event payload delivered when a user types in a `FusionAutoComplete` to filter suggestions.

```csharp
// Properties
Text { get; }  // Search text the user typed.
```

### FusionAutoCompleteFilteringArgsExtensions

Typed mutations on the filtering event args for `FusionAutoComplete`.

```csharp
PreventDefault()
UpdateData<T>()
```

### FusionAutoCompleteHtmlExtensions

Adds typed field mapping and rendering helpers for `AutoCompleteBuilder})`.

```csharp
Fields<T>()
Fields<T>()
FusionAutoComplete<T>()
```

### FusionAutoCompleteReactiveExtensions

Wires `FusionAutoComplete` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionBreadcrumb

Represents a Syncfusion Breadcrumb navigation component.
            Exposes active-item state and item-click payloads; it is not an input component.

### FusionBreadcrumbBuilder<T>

Wraps rendered Syncfusion breadcrumb markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionBreadcrumbEvents

Typed event descriptors for the `FusionBreadcrumb` component.

```csharp
// Properties
ItemClick { get; }  // Fires when a breadcrumb item is clicked.
```

### FusionBreadcrumbExtensions

Reactive pipeline extensions for `FusionBreadcrumb`.

```csharp
ActiveItem<T>()
SetActiveItem<T>()
```

### FusionBreadcrumbHtmlExtensions

Creates typed `FusionBreadcrumbBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionBreadcrumb<T>(html, plan, elementId, build)
```

### FusionBreadcrumbItem

Narrowed breadcrumb item payload proven from Syncfusion Breadcrumb item click behavior.

```csharp
// Properties
Disabled { get; }  // Whether the clicked item is disabled.
IconCss { get; }  // Clicked item's icon CSS classes.
Id { get; }  // Clicked item's id.
Text { get; }  // Clicked item's text.
Url { get; }  // Clicked item's URL.
```

### FusionBreadcrumbItemClickArgs

Event payload delivered when a Breadcrumb item is clicked.

```csharp
// Properties
Item { get; }  // Clicked item metadata from the Syncfusion event.
```

### FusionBreadcrumbReactiveExtensions

Wires `FusionBreadcrumb` events into the Reactive Plan.

### FusionBulletChart

Represents a Syncfusion BulletChart view component.
            Exposes load, tooltip, click, and navigation event helpers; it is not an input component.

### FusionBulletChartBuilder<T>

Wraps BulletChartBuilder.Render() output and carries plan metadata for reactive chaining.

### FusionBulletChartEvents

Typed event descriptors for the `FusionBulletChart` component.

```csharp
// Properties
BulletChartMouseClick { get; }  // Fires when the chart surface is clicked.
TooltipRender { get; }  // Fires before the tooltip renders.
```

### FusionBulletChartExtensions

Reactive pipeline extensions for `FusionBulletChart`.

```csharp
GetActualIndex<T>()
```

### FusionBulletChartHtmlExtensions

Creates typed `FusionBulletChartBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionBulletChart<T>(html, plan, elementId, build)
```

### FusionBulletChartMouseClickArgs

Payload delivered when the BulletChart is clicked.

```csharp
// Properties
Target { get; }  // Clicked target element id.
X { get; }  // Mouse X coordinate in chart space.
Y { get; }  // Mouse Y coordinate in chart space.
```

### FusionBulletChartReactiveExtensions

Wires `FusionBulletChart` events into the Reactive Plan.

### FusionBulletChartTooltipRenderArgs

Payload delivered before the BulletChart tooltip is rendered.

```csharp
// Properties
Name { get; }  // Syncfusion event token exposed as `args.name`.
Target { get; }  // The target values of the comparative bar.
Template { get; }  // The tooltip template markup, when template rendering is enabled.
Text { get; }  // The tooltip text, when template rendering is not enabled.
Value { get; }  // The actual value of the feature bar.
```

### FusionBulletChartTooltipRenderArgsExtensions

Typed mutations on the tooltip render event args for `FusionBulletChart`.

```csharp
SetText()
```

### FusionButton

A FusionButton backed by Syncfusion EJ2 Button.

### FusionButtonBuilder<T>

Wraps Syncfusion ButtonBuilder output while carrying the component id.

```csharp
WriteTo()
```

### FusionButtonExtensions

Typed post-render mutations, reads, and methods for `FusionButton`.

```csharp
Click<T>()
Content<T>()
CssClass<T>()
Disabled<T>()
FocusIn<T>()
IsPrimary<T>()
IsToggle<T>()
SetContent<T>()
SetCssClass<T>()
SetDisabled<T>()
SetIcon<T>()
SetPrimary<T>()
SetToggle<T>()
```

### FusionButtonHtmlExtensions

Creates a FusionButton with Syncfusion MVC builder-owned initial render.

```csharp
FusionButton<T>(html, plan, elementId, build)
```

### FusionButtonIconPosition

Runtime icon placement values supported by Syncfusion EJ2 Button.

### FusionCarousel

Represents a Syncfusion Carousel navigation component.
            Exposes selected-index state, slide navigation methods, and slide-change events; it is not an input component.

### FusionCarouselBuilder<T>

Wraps rendered Syncfusion carousel markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionCarouselEvents

Typed event descriptors for the `FusionCarousel` component.

```csharp
// Properties
SlideChanged { get; }  // Fires after the slide changes.
SlideChanging { get; }  // Fires before the slide changes.
```

### FusionCarouselExtensions

Reactive pipeline extensions for `FusionCarousel`.

```csharp
Next<T>()
Previous<T>()
SelectedIndex<T>()
```

### FusionCarouselHtmlExtensions

Creates typed `FusionCarouselBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionCarousel<T>(html, plan, elementId, build)
```

### FusionCarouselReactiveExtensions

Wires `FusionCarousel` events into the Reactive Plan.

### FusionCarouselSlideChangedArgs

Event payload delivered after a Carousel slide changes.

```csharp
// Properties
CurrentIndex { get; }  // Current slide index.
IsSwiped { get; }  // Whether the slide change was triggered by swipe.
PreviousIndex { get; }  // Previous slide index.
SlideDirection { get; }  // Slide direction: Previous or Next.
```

### FusionCarouselSlideChangingArgs

Event payload delivered before a Carousel slide changes.

```csharp
// Properties
Cancel { get; }  // Whether the slide change should be cancelled.
CurrentIndex { get; }  // Current slide index.
IsSwiped { get; }  // Whether the slide change was triggered by swipe.
NextIndex { get; }  // Next slide index.
SlideDirection { get; }  // Slide direction: Previous or Next.
```

### FusionCarouselSlideChangingArgsExtensions

Typed mutations on the slideChanging event args for `FusionCarousel`.

```csharp
PreventTransition()
```

### FusionCheckBox

A FusionCheckBox backed by Syncfusion EJ2 CheckBox.

```csharp
// Properties
ValueMember { get; }
```

### FusionCheckBoxChangeArgs

Event payload delivered when a `FusionCheckBox` checked state changes.

```csharp
// Properties
Checked { get; }  // Whether the checkbox is checked after the change.
```

### FusionCheckBoxEvents

Typed event descriptors for the `FusionCheckBox` component.

```csharp
// Properties
Changed { get; }  // Fires when the checkbox state changes.
```

### FusionCheckBoxExtensions

Typed post-render mutations, reads, and methods for `FusionCheckBox`.

```csharp
Checked<T>()
Click<T>()
Disabled<T>()
FocusIn<T>()
Indeterminate<T>()
SetChecked<T>()
SetDisabled<T>()
SetIndeterminate<T>()
```

### FusionCheckBoxHtmlExtensions

Creates a FusionCheckBox inside a field wrapper, bound to a boolean model property.

```csharp
FusionCheckBox<T>()
```

### FusionCheckBoxReactiveExtensions

Wires `FusionCheckBox` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionChipItem

A selected chip's bound data, as returned in `getSelectedChips().data` (proven in
            `@syncfusion/ej2-buttons``chip-list.js`: `selectedItems.data.push(this.chips[index])`).
            Each element is the chip model the developer bound, so the array DSL can operate on the
            selection by member — e.g. `p.From(payload, x => x.Selection.Data).Where(c => c.Value == "memory")`.

```csharp
// Properties
Text { get; }  // The chip's display text (`data[i].text`).
Value { get; }  // The chip's bound value (`data[i].value`).
```

### FusionChipList

Syncfusion EJ2 ChipList. Static chip rendering stays on the MVC builder;
            this type scopes typed post-render behavior and event wiring.

### FusionColorPicker

A FusionColorPicker for selecting a color value.

```csharp
// Properties
ValueMember { get; }
```

### FusionColorPickerChangeArgs

Event payload delivered when a `FusionColorPicker` color changes.

```csharp
// Properties
Value { get; }  // Selected color value.
```

### FusionColorPickerEvents

Typed event descriptors for the `FusionColorPicker` component.

```csharp
// Properties
Changed { get; }  // Fires when the color value changes.
```

### FusionColorPickerExtensions

Typed mutations and value reading for `FusionColorPicker` in a reactive pipeline.

```csharp
Disable<T>(disabled)
SetValue<T>(value)
Toggle<T>()
Value<T>()
```

### FusionColorPickerHtmlExtensions

Creates a FusionColorPicker inside a field wrapper, bound to a model property.

```csharp
FusionColorPicker<T>(setup, build)
```

### FusionColorPickerReactiveExtensions

Wires `FusionColorPicker` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionComboBox

A FusionComboBox for selecting or entering a single string value.

```csharp
// Properties
ValueMember { get; }
```

### FusionComboBoxBlurArgs

Event payload delivered when a `FusionComboBox` loses focus.

### FusionComboBoxChangeArgs

Event payload delivered when a `FusionComboBox` selection changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // Selected value.
```

### FusionComboBoxEvents

Typed event descriptors for the `FusionComboBox` component.

```csharp
// Properties
Blur { get; }  // Fires when the component loses focus.
Changed { get; }  // Fires when the selected value changes.
Focus { get; }  // Fires when the component receives focus.
```

### FusionComboBoxExtensions

Typed mutations and value reading for `FusionComboBox` in a reactive pipeline.

```csharp
Clear<T>()
DataBind<T>()
FocusIn<T>()
FocusOut<T>()
HidePopup<T>()
Index<T>()
SetIndex<T>(index)
SetText<T>(text)
SetValue<T>(value)
ShowPopup<T>()
Text<T>()
Value<T>()
```

### FusionComboBoxFocusArgs

Event payload delivered when a `FusionComboBox` receives focus.

### FusionComboBoxHtmlExtensions

Creates a FusionComboBox inside a field wrapper, bound to a model property.

```csharp
FusionComboBox<T>(setup, build)
```

### FusionComboBoxReactiveExtensions

Wires `FusionComboBox` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionContextMenu

Represents a Syncfusion ContextMenu navigation component.
            Exposes open/close methods and lifecycle/item selection payloads; it is not an input component.

### FusionContextMenuBeforeCloseArgs

Payload delivered before the context menu closes.

### FusionContextMenuBeforeCloseArgsExtensions

Typed mutations on the beforeClose event args for `FusionContextMenu`.

### FusionContextMenuBeforeItemRenderArgs

Payload delivered before a context menu item renders.

### FusionContextMenuBeforeOpenArgs

Payload delivered before the context menu opens.

### FusionContextMenuBeforeOpenArgsExtensions

Typed mutations on the beforeOpen event args for `FusionContextMenu`.

### FusionContextMenuBuilder<T>

Wraps ContextMenuBuilder.Render() output and carries plan metadata for reactive chaining.

### FusionContextMenuEvents

Typed event descriptors for the `FusionContextMenu` component.

```csharp
// Properties
BeforeClose { get; }  // Fires before the context menu closes.
BeforeItemRender { get; }  // Fires before each context menu item renders.
BeforeOpen { get; }  // Fires before the context menu opens.
Closed { get; }  // Fires after the context menu closes.
Opened { get; }  // Fires after the context menu opens.
Select { get; }  // Fires when a menu item is selected.
```

### FusionContextMenuExtensions

Reactive pipeline extensions for `FusionContextMenu`.

```csharp
Close<T>()
Open<T>()
```

### FusionContextMenuHtmlExtensions

Creates typed `FusionContextMenuBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionContextMenu<T>(html, plan, elementId, build)
```

### FusionContextMenuItem

Narrow typed context menu item payload projected from Syncfusion ContextMenu item objects.

### FusionContextMenuOpenCloseArgs

Payload delivered after the context menu opens or closes.

### FusionContextMenuReactiveExtensions

Wires `FusionContextMenu` events into the Reactive Plan.

### FusionContextMenuSelectArgs

Payload delivered when a context menu item is selected.

### FusionDatePicker

A FusionDatePicker for selecting a single date.

```csharp
// Properties
ValueMember { get; }
```

### FusionDatePickerChangeArgs

Event payload delivered when a `FusionDatePicker` date changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // New date value.
```

### FusionDatePickerEvents

Typed event descriptors for the `FusionDatePicker` component.

```csharp
// Properties
Changed { get; }  // Fires when the date value changes.
```

### FusionDatePickerExtensions

Typed mutations and value reading for `FusionDatePicker` in a reactive pipeline.

```csharp
FocusIn<T>()
FocusOut<T>()
SetValue<T>(value)
Value<T>()
```

### FusionDatePickerHtmlExtensions

Creates a FusionDatePicker inside a field wrapper, bound to a model property.

```csharp
FusionDatePicker<T>(setup, build)
```

### FusionDatePickerReactiveExtensions

Wires `FusionDatePicker` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionDateRangePicker

A FusionDateRangePicker for selecting a start and end date pair.

```csharp
// Properties
ValueMember { get; }
```

### FusionDateRangePickerChangeArgs

Event payload delivered when a `FusionDateRangePicker` range changes.

```csharp
// Properties
DaySpan { get; }  // Number of days in the selected range.
EndDate { get; }  // End of the selected date range.
IsInteracted { get; }  // Whether user interaction triggered the change.
StartDate { get; }  // Start of the selected date range.
```

### FusionDateRangePickerEvents

Typed event descriptors for the `FusionDateRangePicker` component.

```csharp
// Properties
Changed { get; }  // Fires when the date range value changes.
```

### FusionDateRangePickerExtensions

Typed value reading for `FusionDateRangePicker` in a reactive pipeline.

```csharp
EndDate<T>()
StartDate<T>()
Value<T>()
```

### FusionDateRangePickerHtmlExtensions

Creates a FusionDateRangePicker inside a field wrapper, bound to a model property.

```csharp
FusionDateRangePicker<T>(setup, build)
```

### FusionDateRangePickerReactiveExtensions

Wires `FusionDateRangePicker` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionDateTimePicker

A FusionDateTimePicker for selecting a date and time together.

```csharp
// Properties
ValueMember { get; }
```

### FusionDateTimePickerChangeArgs

Event payload delivered when a `FusionDateTimePicker` value changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // New date-time value.
```

### FusionDateTimePickerEvents

Typed event descriptors for the `FusionDateTimePicker` component.

```csharp
// Properties
Changed { get; }  // Fires when the date-time value changes.
```

### FusionDateTimePickerExtensions

Typed mutations and value reading for `FusionDateTimePicker` in a reactive pipeline.

```csharp
FocusIn<T>()
FocusOut<T>()
SetValue<T>(value)
Value<T>()
```

### FusionDateTimePickerHtmlExtensions

Creates a FusionDateTimePicker inside a field wrapper, bound to a model property.

```csharp
FusionDateTimePicker<T>(setup, build)
```

### FusionDateTimePickerReactiveExtensions

Wires `FusionDateTimePicker` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionDialog

Represents a Syncfusion Dialog display component.
            Exposes show/hide/position methods and lifecycle events; it is not an input component.

### FusionDialogBeforeCloseArgs

Payload for FusionDialog.BeforeClose.
            Fires before the dialog closes.

```csharp
// Properties
Cancel { get; }  // Set to true to prevent the dialog from closing.
IsInteracted { get; }  // True when the user closed the dialog via X button or overlay click.
```

### FusionDialogBeforeOpenArgs

Payload for FusionDialog.BeforeOpen.
            Fires before the dialog opens. Set cancel to true to prevent opening.

```csharp
// Properties
Cancel { get; }  // Set to true to prevent the dialog from opening.
```

### FusionDialogBuilder<T>

Wraps rendered Syncfusion dialog markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionDialogClosedArgs

Payload for FusionDialog.Closed.
            Notification only — fires after the dialog is hidden.

### FusionDialogEvents

Typed event descriptors for the `FusionDialog` component.

```csharp
// Properties
BeforeClose { get; }  // Fires before the dialog closes.
BeforeOpen { get; }  // Fires before the dialog opens.
Closed { get; }  // Fires after the dialog is hidden.
Opened { get; }  // Fires after the dialog is visible.
OverlayClick { get; }  // Fires when the modal overlay is clicked.
```

### FusionDialogExtensions

Mutation extensions for `FusionDialog` in a reactive pipeline.

```csharp
Hide<T>()
RefreshPosition<T>()
Show<T>()
```

### FusionDialogHtmlExtensions

Creates typed `FusionDialogBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionDialog<T>(html, plan, elementId, build)
```

### FusionDialogOpenedArgs

Payload for FusionDialog.Opened.
            Notification only — fires after the dialog is visible.

### FusionDialogOverlayClickArgs

Payload for FusionDialog.OverlayClick.
            Fires when the modal overlay background is clicked.
            Notification only — typically used to close the dialog.

### FusionDialogReactiveExtensions

Wires `FusionDialog` events into the Reactive Plan.

### FusionDropDownButton

Syncfusion EJ2 DropDownButton. Static item and render configuration stays on the MVC builder;
            this type scopes typed post-render behavior and event wiring.

### FusionDropDownButtonBuilder<T>

Wraps Syncfusion DropDownButtonBuilder output while carrying the component id.

```csharp
WriteTo()
```

### FusionDropDownButtonEvents

Typed event descriptors for the `FusionDropDownButton` component.

```csharp
// Properties
Selected { get; }  // Fires after an action item is selected.
```

### FusionDropDownButtonExtensions

Typed post-render mutations, reads, and methods for `FusionDropDownButton`.

```csharp
Content<T>()
CssClass<T>()
Disabled<T>()
FocusIn<T>()
RemoveItemsById<T>()
RemoveItemsByText<T>()
SetContent<T>()
SetCssClass<T>()
SetDisabled<T>()
Toggle<T>()
```

### FusionDropDownButtonHtmlExtensions

Creates a FusionDropDownButton with Syncfusion MVC builder-owned initial render.

```csharp
FusionDropDownButton<T>()
```

### FusionDropDownButtonItem

Narrowed action item payload proven from Syncfusion DropDownButton MenuEventArgs.

```csharp
// Properties
Disabled { get; }  // Whether the selected item is disabled.
Id { get; }  // Selected item's Syncfusion id.
Text { get; }  // Selected item's display text.
```

### FusionDropDownButtonReactiveExtensions

Wires `FusionDropDownButton` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionDropDownButtonSelectArgs

Event payload delivered when a `FusionDropDownButton` action item is selected.

```csharp
// Properties
Item { get; }  // Selected item metadata from the Syncfusion select event.
```

### FusionDropDownList

A FusionDropDownList for selecting a single value from a list.

```csharp
// Properties
ValueMember { get; }
```

### FusionDropDownListBlurArgs

Event payload delivered when a `FusionDropDownList` loses focus.

### FusionDropDownListChangeArgs

Event payload delivered when a `FusionDropDownList` selection changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // Selected value.
```

### FusionDropDownListEvents

Typed event descriptors for the `FusionDropDownList` component.

```csharp
// Properties
Blur { get; }  // Fires when the component loses focus.
Changed { get; }  // Fires when the selected value changes.
Focus { get; }  // Fires when the component receives focus.
```

### FusionDropDownListExtensions

Reactive pipeline extensions for reading and mutating `FusionDropDownList`.

```csharp
DataBind<T>()
FocusIn<T>()
FocusOut<T>()
HidePopup<T>()
SetDataSource<T>()
SetDataSource<T>()
SetDataSource<T>()
SetText<T>()
SetValue<T>(value)
ShowPopup<T>()
Value<T>()
```

### FusionDropDownListFocusArgs

Event payload delivered when a `FusionDropDownList` receives focus.

### FusionDropDownListHtmlExtensions

Adds typed field mapping and rendering helpers for `DropDownListBuilder})`.

```csharp
Fields<T>()
Fields<T>()
FusionDropDownList<T>()
```

### FusionDropDownListReactiveExtensions

Wires `FusionDropDownList` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionDropDownTree

A FusionDropDownTree for selecting values from hierarchical data.

```csharp
// Properties
ValueMember { get; }
```

### FusionDropDownTreeChangeArgs

Event payload delivered when a `FusionDropDownTree` selection changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
OldValue { get; }  // Previous selected value IDs.
Value { get; }  // Selected value IDs.
```

### FusionDropDownTreeEvents

Typed event descriptors for the `FusionDropDownTree` component.

```csharp
// Properties
Changed { get; }  // Fires when the selected value changes.
```

### FusionDropDownTreeExtensions

Typed mutations and value reading for `FusionDropDownTree` in a reactive pipeline.

```csharp
Clear<T>()
DataBind<T>()
HidePopup<T>()
SetText<T>(text)
SetValue<T>(value)
ShowPopup<T>()
Text<T>()
Value<T>()
```

### FusionDropDownTreeHtmlExtensions

Creates a FusionDropDownTree inside a field wrapper, bound to a model property.

```csharp
FusionDropDownTree<T>(setup, build)
```

### FusionDropDownTreeReactiveExtensions

Wires `FusionDropDownTree` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionFileUpload

A FusionFileUpload for selecting files in form mode (no auto-upload).

```csharp
// Properties
ValueMember { get; }
```

### FusionFileUploadEvents

Typed event descriptors for the `FusionFileUpload` component.

```csharp
// Properties
Selected { get; }  // Fires when files are selected.
```

### FusionFileUploadExtensions

Typed value reading for `FusionFileUpload` in a reactive pipeline.

```csharp
Value<T>()
```

### FusionFileUploadHtmlExtensions

Creates a FusionFileUpload inside a field wrapper, bound to a model property.

```csharp
FusionFileUpload<T>(setup, build)
```

### FusionFileUploadReactiveExtensions

Wires `FusionFileUpload` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionFileUploadSelectedArgs

Event payload delivered when files are selected in a `FusionFileUpload`.

```csharp
// Properties
FilesCount { get; }  // Number of files selected.
IsInteracted { get; }  // Whether user interaction triggered the selection.
```

### FusionGrid

FusionGrid — non-input data grid component with server-side custom binding.
            Supports sort, page, and filter via the DataStateChange event.

### FusionGridAction

Action details from the dataStateChange event.
            Contains `requestType` plus context-specific parameters.

```csharp
// Properties
ColumnName { get; }  // Column name for sorting actions.
CurrentPage { get; }  // Current page number after the action.
Direction { get; }  // Sort direction for sorting actions.
PageSize { get; }  // Current page size.
PreviousPage { get; }  // Previous page number before the action.
RequestType { get; }  // Syncfusion request type, such as `sorting` or `paging`.
```

### FusionGridBuilder<T>

Wraps rendered Syncfusion grid markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionGridDataStateChangeArgs

Event args for Syncfusion Grid's `dataStateChange` event.

```csharp
// Properties
Action { get; }  // Action details such as request type, column name, direction, and current page.
Group { get; }  // Active group fields. Empty when ungrouped. Supports nested grouping.
Search { get; }  // Active search descriptors from Grid toolbar or public search method.
Skip { get; }  // Paging offset (0-based). Always present.
Sorted { get; }  // Active sort columns. Empty when unsorted. Supports multi-sort.
Take { get; }  // Page size. Always present.
Where { get; }  // Active text filter criteria from Grid filter UI.
```

### FusionGridDialogFormBuilder<T>

Builds a typed dialog-edit form for a `FusionGrid` row. Each labeled
            field is declared from a typed row expression; the editing row's current value
            binds through the Syncfusion `${field}` template token.

```csharp
Date<T>()
Number<T>()
Select<T>()
Text<T>()
```

### FusionGridEditTemplates

Creates typed `FusionGrid` cell editors rendered as Syncfusion
            `EditTemplate` markup.

```csharp
DateInput<T>()
DialogForm<T>()
Select<T>()
Select<T>()
```

### FusionGridEvents

Typed event descriptors for the `FusionGrid` component.

```csharp
// Properties
DataStateChange { get; }  // Fires when the grid needs data (sort, page, filter). Raised in custom binding mode.
ToolbarClick { get; }  // Fires when any toolbar item is clicked, including custom toolbar buttons. Branch on `args.Item.Id` to handle a specific button.
```

```csharp
ActionBegin<T>()
ActionComplete<T>()
BeforeBatchSave<T>()
BeginEdit<T>()
CellSave<T>()
CellSaved<T>()
RecordClick<T>()
RowSelected<T>()
```

### FusionGridExtensions

Reactive pipeline extensions for `FusionGrid`.

```csharp
AddRecord<T>()
AddRecord<T>()
AutoFitColumn<T>()
AutoFitColumns<T>()
BatchChanges<T>()
ClearFiltering<T>()
ClearGrouping<T>()
ClearSearch<T>()
ClearSelection<T>()
ClearSorting<T>()
CloseEdit<T>()
CsvExport<T>()
CurrentViewRecords<T>()
Data<T>()
DeleteSelectedRecord<T>()
EditCell<T>()
EndEdit<T>()
ExcelExport<T>()
FilterTextBy<T>()
GoToPage<T>()
GroupBy<T>()
HideColumn<T>()
PdfExport<T>()
Print<T>()
Refresh<T>()
ReorderColumnBefore<T>()
RowIndexByPrimaryKey<T>()
SaveCell<T>()
Search<T>()
SelectedRecords<T>()
SelectedRowIndexes<T>()
SelectRow<T>()
SelectRowsByRange<T>()
SetCellValue<T>()
SetCellValue<T>()
SetDataSource<T>()
SetDataSource<T>()
SetDataSource<T>()
SetDataSource<T>()
SetRowData<T>()
SetRowData<T>()
ShowColumn<T>()
ShowColumnChooser<T>()
SortBy<T>()
StartEdit<T>()
UngroupBy<T>()
UpdateRow<T>()
UpdateRow<T>()
```

### FusionGridFieldValidation<T>

Per-field bridge from `ReactiveValidator` client metadata to an EJ2 column
            `validationRules` object, keyed by typed row field.

```csharp
Field<T>()
```

### FusionGridHtmlExtensions

Creates typed `FusionGridBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionGrid<T>(html, plan, elementId, build)
```

### FusionGridReactiveExtensions

Wires `FusionGrid` events into the Reactive Plan.

```csharp
Reactive<T>(builder, eventSelector, pipeline)
```

### FusionGridRecordClickArgs<T>

Event args for the Syncfusion Grid "recordClick" event.

```csharp
// Properties
CellIndex { get; }  // Zero-based cell index.
Name { get; }  // Syncfusion event name.
RowData { get; }  // Current row data.
RowIndex { get; }  // Zero-based row index.
```

### FusionGridRowSelectedArgs<T>

Event args for the Syncfusion Grid "rowSelected" event.

```csharp
// Properties
Data { get; }  // Selected row data.
IsInteracted { get; }  // Whether selection came from user interaction.
PreviousRowIndex { get; }  // Previously selected row index.
RowIndex { get; }  // Selected row index.
```

### FusionGridSearchDescriptor

Search descriptor emitted by Grid dataStateChange for toolbar/search method operations.

```csharp
// Properties
Fields { get; }  // Fields included in the search.
IgnoreAccent { get; }  // Whether search ignores accents.
IgnoreCase { get; }  // Whether search is case-insensitive.
Key { get; }  // Search text.
Operator { get; }  // Search operator such as `contains`.
```

### FusionGridSortColumn

One sort column in the grid's sorted state.
            Syncfusion uses lowercase direction values: `ascending` and `descending`.

```csharp
// Properties
Direction { get; }  // Sort direction: `ascending` or `descending`.
Name { get; }  // Field name being sorted.
```

### FusionGridSortDirection

Sort direction values accepted by Syncfusion EJ2 Grid sortColumn.

### FusionGridTextFilterCriterion

Text filter criterion emitted by Grid dataStateChange for filter-bar/menu text filters.

```csharp
// Properties
Condition { get; }  // Composite predicate condition such as `and` or `or`.
Field { get; }  // Field being filtered.
IgnoreAccent { get; }  // Whether the filter ignores accents.
IgnoreCase { get; }  // Whether the filter ignores case.
MatchCase { get; }  // Whether the filter is case-sensitive.
Operator { get; }  // Filter operator such as `contains`, `equal`, or `startswith`.
Predicate { get; }  // Predicate joining this criterion to the next criterion.
Predicates { get; }  // Nested predicates when Syncfusion emits a composite Predicate.
Value { get; }  // Text value being filtered.
```

### FusionGridTextFilterOperator

Text filter operators accepted by Syncfusion EJ2 Grid filterByColumn.

### FusionGridToolbarClickArgs

Event args for the Syncfusion Grid "toolbarClick" event.
            Fires when any toolbar item is clicked, including custom items.
            Read `Item` to branch on which button was pressed.

```csharp
// Properties
Cancel { get; }  // Set true to cancel the default toolbar action.
Item { get; }  // The clicked toolbar item.
```

### FusionGridToolbarItem

The toolbar item carried by a Grid "toolbarClick" event.
            Custom items expose the id and text declared on the builder Toolbar.

```csharp
// Properties
Id { get; }  // The toolbar item id (e.g. a custom command id, or "{gridId}_excelexport").
Text { get; }  // The toolbar item display text.
```

### FusionGridValidation

Emits Syncfusion-native column `validationRules` from the same`ReactiveValidator` client metadata that powers form validation, so a Grid's
            in-cell editor validates client-side without a second hand-written ruleset. The
            validation source stays single (the validator), and EJ2's own `FormValidator`
            runs the rule on cell save.

```csharp
From<T>()
```

### FusionInPlaceEditor

A FusionInPlaceEditor for click-to-edit single-field commit flows and mixed-form inputs.

```csharp
// Properties
ValueMember { get; }
```

### FusionInPlaceEditorActionBeginArgs

Event payload delivered before a `FusionInPlaceEditor` commits its value.

```csharp
// Properties
Cancel { get; }  // Whether Syncfusion's built-in submit has been cancelled. Set via `IReactionEmitter)`.
Data { get; }  // The payload Syncfusion prepared for its built-in submit, keyed by the editor's `Name`.
```

### FusionInPlaceEditorActionBeginArgsExtensions

Typed mutations on the actionBegin event args for `FusionInPlaceEditor`.

```csharp
PreventDefault(args, pipeline)
```

### FusionInPlaceEditorActionSuccessArgs

Event payload delivered after a successful commit of a `FusionInPlaceEditor`.

```csharp
// Properties
Data { get; }  // Server response data when Syncfusion's UrlAdaptor is configured; empty `{}` when no url.
Name { get; }  // Syncfusion event token exposed as `args.name`.
Value { get; }  // The value that was committed.
```

### FusionInPlaceEditorBeginEditArgs

Event payload delivered before a `FusionInPlaceEditor` enters edit mode.

```csharp
// Properties
Cancel { get; }  // Whether opening the editor is cancelled.
CancelFocus { get; }  // Whether focusing the inner input is cancelled.
Mode { get; }  // The current editor mode ("Inline" or "Popup").
Name { get; }  // Syncfusion event token exposed as `args.name`.
```

### FusionInPlaceEditorBeginEditArgsExtensions

Typed mutations on the beginEdit event args for `FusionInPlaceEditor`.

```csharp
PreventDefault(args, pipeline)
```

### FusionInPlaceEditorCancelClickArgs

Event payload delivered when the user clicks the Cancel button in a `FusionInPlaceEditor`.

```csharp
// Properties
Name { get; }  // Syncfusion event token exposed as `args.name`.
```

### FusionInPlaceEditorChangeArgs

Event payload delivered when the inner editor's value changes.

```csharp
// Properties
Name { get; }  // Syncfusion event token exposed as `args.name`.
PreviousValue { get; }  // The previous value of the inner integrated component.
Value { get; }  // The current value of the inner integrated component (surfaced as string).
```

### FusionInPlaceEditorEndEditArgs

Event payload delivered when a `FusionInPlaceEditor` leaves edit mode.

```csharp
// Properties
Action { get; }  // What ended the edit: `"submit"` on save, `"cancel"` on cancel click.
Cancel { get; }  // Whether leaving edit mode is cancelled.
Mode { get; }  // The current editor mode ("Inline" or "Popup").
Name { get; }  // Syncfusion event token exposed as `args.name`.
```

### FusionInPlaceEditorEndEditArgsExtensions

Typed mutations on the endEdit event args for `FusionInPlaceEditor`.

```csharp
PreventDefault(args, pipeline)
```

### FusionInPlaceEditorEvents

Typed event descriptors for the `FusionInPlaceEditor` component.

```csharp
// Properties
ActionBegin { get; }  // Fires before the submit step. Rarely hooked; set `cancel = true` via `PreventDefault` to block.
ActionSuccess { get; }  // Fires after a successful commit. Primary hook for the reactive POST: only fires after validation passes.
BeginEdit { get; }  // Fires before the editor enters edit mode.
CancelClick { get; }  // Fires on a user cancel click.
Changed { get; }  // Fires when the inner editor's value changes.
EndEdit { get; }  // Fires when the editor leaves edit mode.
SubmitClick { get; }  // Fires on a user save click or Enter key. Fires even when validation blocks the commit; use `ActionSuccess` for post-commit side effects.
```

### FusionInPlaceEditorExtensions

Reactive pipeline extensions for reading and mutating `FusionInPlaceEditor`.

```csharp
AddClass<T>(className)
Disable<T>()
Enable<T>()
Focus<T>()
RemoveClass<T>(className)
Save<T>()
SetValue<T>(value)
Value<T>()
```

### FusionInPlaceEditorHtmlExtensions

Creates a FusionInPlaceEditor inside a field wrapper, bound to a model property.

```csharp
FusionInPlaceEditor<T>(setup, build)
```

### FusionInPlaceEditorReactiveExtensions

Wires `FusionInPlaceEditor` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionInPlaceEditorSubmitClickArgs

Event payload delivered when the user clicks Save or presses Enter in a `FusionInPlaceEditor`.

```csharp
// Properties
Name { get; }  // Syncfusion event token exposed as `args.name`.
```

### FusionInputMask

A FusionInputMask for entering text with a format mask (e.g. phone numbers).

```csharp
// Properties
ValueMember { get; }
```

### FusionInputMaskChangeArgs

Event payload delivered when a `FusionInputMask` value changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // New masked value.
```

### FusionInputMaskEvents

Typed event descriptors for the `FusionInputMask` component.

```csharp
// Properties
Changed { get; }  // Fires when the masked value changes.
```

### FusionInputMaskExtensions

Typed mutations and value reading for `FusionInputMask` in a reactive pipeline.

```csharp
FocusIn<T>()
SetValue<T>(value)
Value<T>()
```

### FusionInputMaskHtmlExtensions

Creates a FusionInputMask inside a field wrapper, bound to a model property.

```csharp
FusionInputMask<T>(setup, build)
```

### FusionInputMaskReactiveExtensions

Wires `FusionInputMask` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionKanban

Syncfusion EJ2 Kanban. Initial board configuration stays on Syncfusion's
            KanbanBuilder; this type scopes typed post-render behavior for Reactive Plans.

### FusionKanbanBuilder<T>

Carries rendered Syncfusion Kanban markup and the Reactive Plan for event wiring.

### FusionKanbanEvents

Typed Kanban events available to Reactive Plans.

### FusionKanbanExtensions

Runtime behavior for Syncfusion Kanban. Initial board setup remains on
            Syncfusion's KanbanBuilder; these members cover post-render reads,
            mutations, and method-return sources.

```csharp
SetDataSource<T>()
```

### FusionKanbanHtmlExtensions

Factory extension for rendering Syncfusion Kanban with reactive event support.
            Static board setup stays on Syncfusion's MVC builder.

### FusionKanbanReactiveExtensions

Wires `FusionKanban` events into the Reactive Plan.

### FusionListBox

Syncfusion EJ2 ListBox. Static rendering and data binding stay on the MVC builder;
            this type scopes proven post-render string-value behavior and event wiring.

### FusionListBoxBuilder<T>

Wraps Syncfusion ListBox render output while carrying Reactive Plan context.

### FusionListBoxChangeArgs

Event payload delivered when a `FusionListBox` selection changes.

```csharp
// Properties
Value { get; }  // Selected values.
```

### FusionListBoxEvents

Typed event descriptors for the `FusionListBox` component.

```csharp
// Properties
Changed { get; }  // Fires when selected values change.
```

### FusionListBoxExtensions

Reactive pipeline extensions for `FusionListBox`.

```csharp
DataBind<T>()
DisableValues<T>()
EnableValues<T>()
SelectAll<T>()
SelectValues<T>()
SetValue<T>()
UnselectAll<T>()
UnselectValues<T>()
Value<T>()
```

### FusionListBoxHtmlExtensions

Creates a FusionListBox with Syncfusion MVC builder-owned static configuration.

```csharp
FusionListBox<T>(html, plan, elementId, build)
```

### FusionListBoxReactiveExtensions

Wires `FusionListBox` events into the Reactive Plan.

### FusionListView

Syncfusion EJ2 ListView. Static rendering and data binding stay on the MVC builder;
            this type scopes proven post-render behavior and event wiring.

### FusionListViewBuilder<T>

Wraps Syncfusion ListView render output while carrying Reactive Plan context.

### FusionListViewEvents

Typed event descriptors for the `FusionListView` component.

```csharp
// Properties
Selected { get; }  // Fires when a ListView item is selected.
```

### FusionListViewExtensions

Reactive pipeline extensions for `FusionListView`.

```csharp
CheckAllItems<T>()
SelectText<T>()
UncheckAllItems<T>()
UnselectText<T>()
```

### FusionListViewHtmlExtensions

Creates a FusionListView with Syncfusion MVC builder-owned static configuration.

```csharp
FusionListView<T>(html, plan, elementId, build)
```

### FusionListViewReactiveExtensions

Wires `FusionListView` events into the Reactive Plan.

### FusionListViewSelectArgs

Event payload delivered when a `FusionListView` item is selected.

```csharp
// Properties
Cancel { get; }  // Whether Syncfusion should cancel the selection.
Index { get; }  // Selected item's zero-based index when the event came from user interaction.
IsChecked { get; }  // Checkbox state for checkbox ListViews.
IsInteracted { get; }  // Whether the selection came from user interaction.
Text { get; }  // Selected item's visible text.
```

### FusionMention

Syncfusion EJ2 Mention. The MVC builder owns static list configuration;
            this type scopes typed post-render behavior and event wiring.

### FusionMenu

Represents a Syncfusion Menu navigation component.
            Exposes open/close methods and lifecycle/item selection payloads; it is not an input component.

### FusionMenuBeforeCloseArgs

Payload delivered before the menu closes.

### FusionMenuBeforeCloseArgsExtensions

Typed mutations on the beforeClose event args for `FusionMenu`.

### FusionMenuBeforeItemRenderArgs

Payload delivered before a menu item renders.

### FusionMenuBeforeOpenArgs

Payload delivered before the menu opens.

### FusionMenuBeforeOpenArgsExtensions

Typed mutations on the beforeOpen event args for `FusionMenu`.

### FusionMenuBuilder<T>

Wraps MenuBuilder.Render() output and carries plan metadata for reactive chaining.

### FusionMenuEvents

Typed event descriptors for the `FusionMenu` component.

```csharp
// Properties
BeforeClose { get; }  // Fires before the menu closes.
BeforeItemRender { get; }  // Fires before each menu item renders.
BeforeOpen { get; }  // Fires before the menu opens.
Closed { get; }  // Fires after the menu closes.
Opened { get; }  // Fires after the menu opens.
Select { get; }  // Fires when a menu item is selected.
```

### FusionMenuExtensions

Reactive pipeline extensions for `FusionMenu`.

```csharp
Close<T>()
Open<T>()
```

### FusionMenuHtmlExtensions

Creates typed `FusionMenuBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionMenu<T>(html, plan, elementId, build)
```

### FusionMenuItem

Narrow typed menu item payload projected from Syncfusion Menu item objects.

### FusionMenuOpenCloseArgs

Payload delivered after the menu opens or closes.

### FusionMenuReactiveExtensions

Wires `FusionMenu` events into the Reactive Plan.

### FusionMenuSelectArgs

Payload delivered when a menu item is selected.

### FusionMultiColumnComboBox

A FusionMultiColumnComboBox for selecting a value with a multi-column dropdown.

```csharp
// Properties
ValueMember { get; }
```

### FusionMultiColumnComboBoxChangeArgs

Event payload delivered when a `FusionMultiColumnComboBox` selection changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // Selected value.
```

### FusionMultiColumnComboBoxEvents

Typed event descriptors for the `FusionMultiColumnComboBox` component.

```csharp
// Properties
Changed { get; }  // Fires when the selected value changes.
```

### FusionMultiColumnComboBoxExtensions

Reactive pipeline extensions for reading and mutating `FusionMultiColumnComboBox`.

```csharp
DataBind<T>()
FocusIn<T>()
FocusOut<T>()
HidePopup<T>()
SetDataSource<T>()
SetDataSource<T>()
SetDataSource<T>()
SetText<T>()
SetValue<T>(value)
ShowPopup<T>()
Value<T>()
```

### FusionMultiColumnComboBoxHtmlExtensions

Adds typed field mapping and rendering helpers for `MultiColumnComboBoxBuilder})`.

```csharp
Fields<T>()
Fields<T>()
FusionMultiColumnComboBox<T>()
```

### FusionMultiColumnComboBoxReactiveExtensions

Wires `FusionMultiColumnComboBox` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionMultiSelect

A FusionMultiSelect for choosing multiple values from a list.

```csharp
// Properties
ValueMember { get; }
```

### FusionMultiSelectChangeArgs

Event payload delivered when a `FusionMultiSelect` selection changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // Selected values.
```

### FusionMultiSelectEvents

Typed event descriptors for the `FusionMultiSelect` component.

```csharp
// Properties
Changed { get; }  // Fires when the selected value array changes.
Filtering { get; }  // Fires when the user types to filter.
```

### FusionMultiSelectExtensions

Reactive pipeline extensions for reading and mutating `FusionMultiSelect`.

```csharp
DataBind<T>()
HidePopup<T>()
SetDataSource<T>(source, path)
SetDataSource<T>(source, path)
SetDataSource<T>(source)
SetValue<T>(value)
ShowPopup<T>()
Value<T>()
```

### FusionMultiSelectFilteringArgs

Event payload delivered when a user types in a `FusionMultiSelect` to filter suggestions.

```csharp
// Properties
Text { get; }  // Search text the user typed.
```

### FusionMultiSelectFilteringArgsExtensions

Typed mutations on the filtering event args for `FusionMultiSelect`.

```csharp
PreventDefault(args, pipeline)
UpdateData<T>(args, pipeline, source, path)
```

### FusionMultiSelectHtmlExtensions

Creates a FusionMultiSelect inside a field wrapper, bound to a model property.

```csharp
Fields<T>(builder, text, value)
Fields<T>(builder, text, value, groupBy)
FusionMultiSelect<T>(setup, build)
```

### FusionMultiSelectReactiveExtensions

Wires `FusionMultiSelect` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionNumericTextBox

A FusionNumericTextBox for entering and validating numeric values.

```csharp
// Properties
ValueMember { get; }
```

### FusionNumericTextBoxBlurArgs

Event payload delivered when a `FusionNumericTextBox` loses focus.

### FusionNumericTextBoxChangeArgs

Event payload delivered when a `FusionNumericTextBox` value changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
PreviousValue { get; }  // Previous numeric value.
Value { get; }  // New numeric value.
```

### FusionNumericTextBoxEvents

Typed event descriptors for the `FusionNumericTextBox` component.

```csharp
// Properties
Blur { get; }  // Fires when the component loses focus.
Changed { get; }  // Fires when the numeric value changes.
Focus { get; }  // Fires when the component receives focus.
```

### FusionNumericTextBoxExtensions

Typed mutations and value reading for `FusionNumericTextBox` in a reactive pipeline.

```csharp
Decrement<T>()
FocusIn<T>()
FocusOut<T>()
Increment<T>()
SetMin<T>()
SetValue<T>()
Value<T>()
```

### FusionNumericTextBoxFocusArgs

Event payload delivered when a `FusionNumericTextBox` receives focus.

### FusionNumericTextBoxHtmlExtensions

Adds rendering helpers for `NumericTextBoxBuilder})`.

```csharp
FusionNumericTextBox<T>()
```

### FusionNumericTextBoxReactiveExtensions

Wires `FusionNumericTextBox` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionOtpInput

A FusionOtpInput for entering a one-time passcode value.

```csharp
// Properties
ValueMember { get; }
```

### FusionOtpInputBlurArgs

Event payload delivered when a `FusionOtpInput` field loses focus.

```csharp
// Properties
Index { get; }  // Zero-based field index that lost focus.
IsInteracted { get; }  // Whether user interaction caused the blur.
Value { get; }  // Current OTP value.
```

### FusionOtpInputEvents

Typed event descriptors for the `FusionOtpInput` component.

```csharp
// Properties
Blur { get; }  // Fires when an OTP field loses focus.
Focus { get; }  // Fires when an OTP field receives focus.
Input { get; }  // Fires as an OTP field changes while editing.
ValueChanged { get; }  // Fires after the complete OTP value changes.
```

### FusionOtpInputExtensions

Typed mutations, methods, and value reading for `FusionOtpInput` in a reactive pipeline.

```csharp
FocusIn<T>()
FocusOut<T>()
SetValue<T>()
Value<T>()
```

### FusionOtpInputFocusArgs

Event payload delivered when a `FusionOtpInput` field receives focus.

```csharp
// Properties
Index { get; }  // Zero-based field index that received focus.
IsInteracted { get; }  // Whether user interaction caused the focus.
Value { get; }  // Current OTP value.
```

### FusionOtpInputHtmlExtensions

Adds rendering helpers for `OtpInputBuilder})`.

```csharp
FusionOtpInput<T>()
```

### FusionOtpInputInputArgs

Event payload delivered when a `FusionOtpInput` field changes.

```csharp
// Properties
Index { get; }  // Zero-based field index that changed.
PreviousValue { get; }  // Previous OTP value.
Value { get; }  // Current OTP value.
```

### FusionOtpInputReactiveExtensions

Wires `FusionOtpInput` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionOtpInputValueChangedArgs

Event payload delivered when a complete `FusionOtpInput` value changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
PreviousValue { get; }  // Previous committed OTP value.
Value { get; }  // New OTP value.
```

### FusionPivotView

Syncfusion EJ2 PivotView. Static report setup stays on the Syncfusion MVC builder;
            this type scopes typed post-render behavior for Reactive Plans.

### FusionPivotViewBuilder<T>

Carries rendered Syncfusion PivotView markup and the Reactive Plan for event wiring.

### FusionPivotViewCellData

Pivot cell data exposed by Syncfusion inside cellSelecting args.

### FusionPivotViewCellSelectingArgs

Payload for PivotView cellSelecting. This event is emitted during normal
            late event subscription, which matches Reactive Plan runtime wiring.

### FusionPivotViewDataBoundArgs

Payload for PivotView dataBound. Syncfusion raises this after the pivot data renders.

### FusionPivotViewEvents

Typed PivotView events available to Reactive Plans.

### FusionPivotViewExtensions

Runtime behavior for Syncfusion PivotView. Initial report configuration stays on
            Syncfusion's PivotViewBuilder.

### FusionPivotViewHtmlExtensions

Factory extension for rendering Syncfusion PivotView with reactive event support.

### FusionPivotViewReactiveExtensions

Wires `FusionPivotView` events into the Reactive Plan.

### FusionProgressButton

Syncfusion EJ2 ProgressButton. Static render configuration stays on the MVC builder;
            this type scopes typed post-render progress behavior and event wiring.

### FusionProgressButtonBuilder<T>

Wraps Syncfusion ProgressButtonBuilder output while carrying the component id.

```csharp
WriteTo()
```

### FusionProgressButtonEvents

Typed event descriptors for the `FusionProgressButton` component.

```csharp
// Properties
Began { get; }  // Fires when progress starts.
Ended { get; }  // Fires when progress completes.
Progressed { get; }  // Fires as progress advances.
```

### FusionProgressButtonExtensions

Typed post-render mutations, reads, and methods for `FusionProgressButton`.

```csharp
Content<T>()
CssClass<T>()
Disabled<T>()
FocusIn<T>()
ProgressComplete<T>()
ProgressEnabled<T>()
SetContent<T>()
SetCssClass<T>()
SetDisabled<T>()
SetProgressEnabled<T>()
Start<T>()
StartAt<T>()
```

### FusionProgressButtonHtmlExtensions

Creates a FusionProgressButton with Syncfusion MVC builder-owned initial render.

```csharp
FusionProgressButton<T>()
```

### FusionProgressButtonProgressArgs

Event payload delivered by ProgressButton begin, progress, and end events.

```csharp
// Properties
CurrentDuration { get; }  // Current progress duration in milliseconds.
Percent { get; }  // Current progress percentage.
Step { get; }  // Progress step interval.
```

### FusionProgressButtonReactiveExtensions

Wires `FusionProgressButton` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionRadioButton

Syncfusion EJ2 RadioButton. Static option rendering stays on the MVC builder;
            this type scopes typed post-render behavior and event wiring.

### FusionRadioButtonBuilder<T>

Wraps Syncfusion RadioButtonBuilder output while carrying the component id.

```csharp
WriteTo()
```

### FusionRadioButtonChangeArgs

Event payload delivered when a `FusionRadioButton` is selected.

```csharp
// Properties
Value { get; }  // Selected radio button value from the Syncfusion change event.
```

### FusionRadioButtonEvents

Typed event descriptors for the `FusionRadioButton` component.

```csharp
// Properties
Changed { get; }  // Fires when this radio button becomes selected.
```

### FusionRadioButtonExtensions

Typed post-render mutations, reads, and methods for `FusionRadioButton`.

```csharp
Checked<T>()
Click<T>()
Disabled<T>()
FocusIn<T>()
SelectedValue<T>()
SetChecked<T>()
SetDisabled<T>()
```

### FusionRadioButtonHtmlExtensions

Creates a FusionRadioButton with Syncfusion MVC builder-owned initial render.

```csharp
FusionRadioButton<T>()
```

### FusionRadioButtonReactiveExtensions

Wires `FusionRadioButton` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionRating

A FusionRating for selecting a numeric rating value.

```csharp
// Properties
ValueMember { get; }
```

### FusionRatingEvents

Typed event descriptors for the `FusionRating` component.

```csharp
// Properties
ValueChanged { get; }  // Fires when the rating value changes.
```

### FusionRatingExtensions

Typed mutations, methods, and value reading for `FusionRating` in a reactive pipeline.

```csharp
Reset<T>()
SetValue<T>(value)
Value<T>()
```

### FusionRatingHtmlExtensions

Creates a FusionRating inside a field wrapper, bound to a numeric model property.

```csharp
FusionRating<T>(setup, build)
```

### FusionRatingReactiveExtensions

Wires `FusionRating` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionRatingValueChangedArgs

Event payload delivered when a `FusionRating` value changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
PreviousValue { get; }  // Previous rating value.
Value { get; }  // New rating value.
```

### FusionRichTextEditor

A FusionRichTextEditor for editing HTML content.

```csharp
// Properties
ValueMember { get; }
```

### FusionRichTextEditorChangeArgs

Event payload delivered when a `FusionRichTextEditor` content changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // New HTML content value.
```

### FusionRichTextEditorEvents

Typed event descriptors for the `FusionRichTextEditor` component.

```csharp
// Properties
Changed { get; }  // Fires when the rich text content changes.
```

### FusionRichTextEditorExtensions

Typed mutations and value reading for `FusionRichTextEditor` in a reactive pipeline.

```csharp
FocusIn<T>()
SetValue<T>(value)
Value<T>()
```

### FusionRichTextEditorHtmlExtensions

Creates a FusionRichTextEditor inside a field wrapper, bound to a model property.

```csharp
FusionRichTextEditor<T>(setup, build)
```

### FusionRichTextEditorReactiveExtensions

Wires `FusionRichTextEditor` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionSchedule

Represents a Syncfusion Schedule display component.
            Exposes scheduler methods and lifecycle events; it is not an input component.

### FusionScheduleActionBeginArgs

Event payload delivered before Syncfusion Schedule starts an action.
            Set `Cancel` to prevent the action. `RequestType`
            identifies the action, for example `eventCreate`, `eventChange`, etc.

```csharp
// Properties
Cancel { get; }  // Set to true before the callback returns to cancel the action.
RequestType { get; }  // Syncfusion action name, for example `eventCreate` or `dateNavigate`.
```

### FusionScheduleActionCompleteArgs

Event payload delivered after Syncfusion Schedule completes an action successfully.

```csharp
// Properties
RequestType { get; }  // Syncfusion action name that completed, for example `eventChange` or `viewNavigate`.
```

### FusionScheduleBuilder<T>

Wraps rendered Syncfusion schedule markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionScheduleCellClickArgs

Event payload delivered when a Syncfusion Schedule time cell is clicked.
            Includes the selected slot and resource group.

```csharp
// Properties
EndTime { get; }  // End time of the clicked cell slot.
GroupIndex { get; }  // Zero-based index of the resource group (e.g., which shift).
IsAllDay { get; }  // True if the all-day row was clicked.
StartTime { get; }  // Start time of the clicked cell slot.
```

### FusionScheduleDataBoundArgs

Notification payload delivered after Syncfusion Schedule data has loaded and rendered.
            Use to hide loaders or update counts after server data loads.

### FusionScheduleEventClickArgs

Event payload delivered when a Syncfusion Schedule appointment is clicked.

```csharp
// Properties
Cancel { get; }  // Set to true before the callback returns to prevent the default quick-info popup.
```

### FusionScheduleEventData

Schedule event data projected from Syncfusion event records and `getEvents()`.

```csharp
// Properties
CategoryColor { get; }  // Optional color used by Syncfusion when rendering the appointment.
Id { get; }  // Schedule event identifier.
IsUnassigned { get; }  // Whether the appointment currently has no assigned staff member.
ShiftId { get; }  // Resource or group identifier for the shift.
StaffName { get; }  // Assigned staff member name when available.
StaffRole { get; }  // Assigned staff member role when available.
Subject { get; }  // Display title for the appointment.
```

### FusionScheduleEventRenderedArgs

Event payload delivered before Syncfusion Schedule renders an appointment.
            Use `Data` for conditional styling. Set `Cancel`
            to prevent the appointment from rendering.

```csharp
// Properties
Cancel { get; }  // Set to true before the callback returns to prevent this appointment from rendering.
Data { get; }  // Schedule event data being rendered. Use in conditions for conditional styling.
Type { get; }  // Syncfusion render type, for example `event`.
```

### FusionScheduleEvents

Typed event descriptors for the `FusionSchedule` component.

```csharp
// Properties
ActionBegin { get; }  // Fires before a scheduler action begins. Use `RequestType` to distinguish `eventCreate`, `eventChange`, etc.
ActionComplete { get; }  // Fires after a scheduler action completes. Use `RequestType` to identify the completed action.
CellClicked { get; }  // Fires when a time cell is clicked. Includes start/end time, group index, and all-day state.
DataBound { get; }  // Fires after data is loaded and rendered.
EventClicked { get; }  // Fires when an event/appointment is clicked.
EventRendered { get; }  // Fires before each event renders. Used for custom styling (e.g., unassigned shifts in red). Cancelable.
Navigating { get; }  // Fires before date or view navigation. Use `Action` to distinguish `date` and `view` navigation. Cancelable.
PopupClose { get; }  // Fires when a popup closes.
PopupOpen { get; }  // Fires before a popup opens. Use `Type` to distinguish `QuickInfo`, `Editor`, etc. Cancelable.
```

### FusionScheduleExtensions

Reactive pipeline extensions for reading and mutating `FusionSchedule`.

```csharp
AddEvent<T>()
CloseEditor<T>()
CurrentView<T>()
DeleteEvent<T>()
GetEvents<T>()
OpenEditor<T>()
Print<T>()
RefreshEvents<T>()
SaveEvent<T>()
ScrollTo<T>()
SelectedDate<T>()
SetDataSource<T>()
SetDataSource<T>()
```

### FusionScheduleHtmlExtensions

Creates typed `FusionScheduleBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionSchedule<T>(html, plan, elementId, build)
```

### FusionScheduleNavigatingArgs

Event payload delivered before Syncfusion Schedule changes date or view.
            Set `Cancel` to prevent navigation.

```csharp
// Properties
Action { get; }  // Navigation action, either `date` or `view`.
Cancel { get; }  // Set to true before the callback returns to cancel navigation.
CurrentDate { get; }  // Target date as an ISO string reported by Syncfusion.
CurrentView { get; }  // Target view, for example `Day` or `Week`. Empty on date navigation.
PreviousDate { get; }  // Previous date as an ISO string reported by Syncfusion.
PreviousView { get; }  // Previous view when switching views. Empty on date navigation.
ViewIndex { get; }  // Zero-based index of the target view when switching views.
```

### FusionSchedulePopupCloseArgs

Event payload delivered after a Syncfusion Schedule popup closes.

```csharp
// Properties
Type { get; }  // Popup type that closed, for example `QuickInfo` or `Editor`.
```

### FusionSchedulePopupData

Schedule event subset available while a Syncfusion Schedule popup opens.

```csharp
// Properties
Id { get; }  // Schedule event identifier.
Subject { get; }  // Display title for the appointment.
```

### FusionSchedulePopupOpenArgs

Event payload delivered before Syncfusion Schedule opens a popup.
            Set `Cancel` to prevent the popup.

```csharp
// Properties
Cancel { get; }  // Set to true before the callback returns to prevent the popup from opening.
Data { get; }  // Schedule event data associated with this popup when Syncfusion provides it.
Type { get; }  // Popup type, for example `QuickInfo` or `Editor`.
```

### FusionSchedulePopupOpenArgsExtensions

Typed mutations for Syncfusion Schedule `popupOpen` event args.

```csharp
PreventDefault()
```

### FusionScheduleReactiveExtensions

Wires `FusionSchedule` events into the Reactive Plan.

```csharp
Reactive<T>(builder, eventSelector, pipeline)
```

### FusionSelectedChips

The result of `ChipList.getSelectedChips()` for a multiple-selection chip list. Mirrors
            the shipped shape `{ texts, Indexes, data, elements }` (chip-list.js). `Data`
            carries the selected chip objects, so the array DSL can filter/aggregate the selection by
            member; `Texts` carries the selected display strings.

```csharp
// Properties
Data { get; }  // The selected chips' bound data objects (`data`) — the array DSL source for operating on the selection by member (text/value).
Indexes { get; }  // The selected chip positions from Syncfusion's selection payload.
Texts { get; }  // The selected chips' display text (`texts`).
```

### FusionSidebar

Represents a Syncfusion Sidebar navigation component.
            Exposes open-state reads, show/hide/toggle methods, and transition events;
            it is not an input component.

### FusionSidebarBuilder<T>

Wraps rendered Syncfusion sidebar markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionSidebarEvents

Typed event descriptors for the `FusionSidebar` component.

```csharp
// Properties
Closed { get; }  // Selects the Syncfusion `close` transition event.
Opened { get; }  // Selects the Syncfusion `open` transition event.
```

### FusionSidebarExtensions

Component actions and value sources available inside Reactive Plan pipelines for `FusionSidebar`.

```csharp
Hide<T>()
IsOpen<T>()
Show<T>()
Toggle<T>()
```

### FusionSidebarHtmlExtensions

Creates typed `FusionSidebarBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionSidebar<T>(html, plan, elementId, build)
```

### FusionSidebarReactiveExtensions

Wires `FusionSidebar` events into the Reactive Plan.

```csharp
Reactive<T>(builder, eventSelector, pipeline)
```

### FusionSidebarTransitionArgs

Event payload delivered before a Sidebar transition.

```csharp
// Properties
IsInteracted { get; }  // Whether Syncfusion reports user interaction for this transition.
```

### FusionSlider

A FusionSlider for selecting a numeric value or numeric range.

```csharp
// Properties
ValueMember { get; }
```

### FusionSliderChangeArgs

Event payload delivered when a scalar `FusionSlider` value changes.

```csharp
// Properties
Action { get; }  // Syncfusion action name for the change.
IsInteracted { get; }  // Whether user interaction triggered the change.
PreviousValue { get; }  // Previous slider value.
Text { get; }  // Formatted slider value text.
Value { get; }  // New slider value.
```

### FusionSliderEvents

Typed event descriptors for the `FusionSlider` component.

```csharp
// Properties
Change { get; }  // Fires when the scalar slider value changes.
Changed { get; }  // Fires after the scalar slider value changes.
```

### FusionSliderExtensions

Typed mutations and value reading for `FusionSlider` in a reactive pipeline.

```csharp
RangeValue<T>()
SetRangeValue<T>(start, end)
SetValue<T>(value)
Value<T>()
```

### FusionSliderHtmlExtensions

Creates a FusionSlider inside a field wrapper, bound to a model property.

```csharp
FusionSlider<T>(setup, build)
```

### FusionSliderReactiveExtensions

Wires `FusionSlider` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionSmartPasteButton

Syncfusion EJ2 SmartPasteButton. Syncfusion does not ship an MVC builder for this
            package version, so this vertical slice owns the typed render helper.

### FusionSmartTextArea

Syncfusion EJ2 SmartTextArea. Syncfusion does not ship an MVC builder for this
            package version, so this vertical slice owns the typed render helper.

### FusionSplitButton

Syncfusion EJ2 SplitButton. Static render configuration stays on the MVC builder;
            this type scopes typed post-render behavior and event wiring.

### FusionSplitButtonBuilder<T>

Wraps Syncfusion SplitButtonBuilder output while carrying the component id.

```csharp
WriteTo()
```

### FusionSplitButtonClickArgs

Event payload delivered when a `FusionSplitButton` primary action is clicked.
            Syncfusion exposes the clicked HTMLElement, which is intentionally not surfaced in the public API.

### FusionSplitButtonEvents

Typed event descriptors for the `FusionSplitButton` component.

```csharp
// Properties
Clicked { get; }  // Fires after the primary SplitButton action is clicked.
Selected { get; }  // Fires after a secondary action item is selected.
```

### FusionSplitButtonExtensions

Typed post-render mutations, reads, and methods for `FusionSplitButton`.

```csharp
Content<T>()
CssClass<T>()
Disabled<T>()
FocusIn<T>()
RemoveItemsById<T>()
RemoveItemsByText<T>()
SetContent<T>()
SetCssClass<T>()
SetDisabled<T>()
Toggle<T>()
```

### FusionSplitButtonHtmlExtensions

Creates a FusionSplitButton with Syncfusion MVC builder-owned initial render.

```csharp
FusionSplitButton<T>()
```

### FusionSplitButtonItem

Narrowed action item payload proven from Syncfusion SplitButton MenuEventArgs.

```csharp
// Properties
Disabled { get; }  // Whether the selected item is disabled.
Id { get; }  // Selected item's Syncfusion id.
Text { get; }  // Selected item's display text.
```

### FusionSplitButtonReactiveExtensions

Wires `FusionSplitButton` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionSplitButtonSelectArgs

Event payload delivered when a `FusionSplitButton` secondary action item is selected.

```csharp
// Properties
Item { get; }  // Selected item metadata from the Syncfusion select event.
```

### FusionStepper

Represents a Syncfusion Stepper progress/navigation component.
            Exposes active-step state, navigation methods, and step-change events; it is not an input component.

### FusionStepperBuilder<T>

Wraps StepperBuilder.Render() output and carries plan metadata for reactive chaining.

### FusionStepperChangedArgs

Event payload delivered after a Stepper step changes.

```csharp
// Properties
ActiveStep { get; }  // The index of the current step.
IsInteracted { get; }  // Whether the change was triggered by user interaction.
PreviousStep { get; }  // The index of the previous step.
```

### FusionStepperChangingArgs

Event payload delivered before a Stepper step changes.

```csharp
// Properties
ActiveStep { get; }  // The index of the target step.
Cancel { get; }  // Whether Syncfusion should cancel the step transition.
IsInteracted { get; }  // Whether the change was triggered by user interaction.
PreviousStep { get; }  // The index of the previous step.
```

### FusionStepperChangingArgsExtensions

Typed mutations on the stepChanging event args for `FusionStepper`.

```csharp
PreventDefault()
```

### FusionStepperClickArgs

Event payload delivered when a Stepper step is clicked.

```csharp
// Properties
ActiveStep { get; }  // The index of the current step.
PreviousStep { get; }  // The index of the previous step.
```

### FusionStepperEvents

Typed event descriptors for the `FusionStepper` component.

```csharp
// Properties
StepChanged { get; }  // Fires after the active step changes.
StepChanging { get; }  // Fires before the active step changes.
StepClick { get; }  // Fires when a step is clicked.
```

### FusionStepperExtensions

Typed post-render navigation methods for `FusionStepper`.

```csharp
ActiveStep<T>()
NextStep<T>()
PreviousStep<T>()
RefreshProgressbar<T>()
Reset<T>()
SetActiveStep<T>()
```

### FusionStepperHtmlExtensions

Creates typed `FusionStepperBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionStepper<T>(html, plan, elementId, build)
```

### FusionStepperReactiveExtensions

Wires `FusionStepper` events into the Reactive Plan.

### FusionSwitch

A FusionSwitch for toggling a boolean on/off.

```csharp
// Properties
ValueMember { get; }
```

### FusionSwitchChangeArgs

Event payload delivered when a `FusionSwitch` state changes.

```csharp
// Properties
Checked { get; }  // Switch checked state after the change.
IsInteracted { get; }  // Whether user interaction triggered the change.
```

### FusionSwitchEvents

Typed event descriptors for the `FusionSwitch` component.

```csharp
// Properties
Changed { get; }  // Fires when the switch state changes.
```

### FusionSwitchExtensions

Typed mutations and value reading for `FusionSwitch` in a reactive pipeline.

```csharp
SetChecked<T>()
Value<T>()
```

### FusionSwitchHtmlExtensions

Adds rendering helpers for `SwitchBuilder})`.

```csharp
FusionSwitch<T>()
```

### FusionSwitchReactiveExtensions

Wires `FusionSwitch` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionTab

Represents a Syncfusion Tab navigation component.
            Exposes selected-tab state, tab visibility methods, and selection events; it is not an input component.

### FusionTabBuilder<T>

Carries rendered Syncfusion Tab markup and the Reactive Plan for event wiring.
            Non-input component: no InputField wrapper, label, or validation slot.

### FusionTabEvents

Typed event descriptors for the `FusionTab` component.

```csharp
// Properties
Selected { get; }  // Fires when a tab is selected.
```

### FusionTabExtensions

Mutation extensions for `FusionTab` in a reactive pipeline.

```csharp
HideTab<T>()
Select<T>()
SetSelectedItem<T>()
```

### FusionTabHtmlExtensions

Creates typed `FusionTabBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionTab<T>(html, plan, elementId, build)
```

### FusionTabReactiveExtensions

Wires `FusionTab` events into the Reactive Plan.

```csharp
Reactive<T>(builder, eventSelector, pipeline)
```

### FusionTabSelectedArgs

Event args for the Syncfusion Tab "selected" event.
             Fires when a tab is selected.

             Properties are primitives — safe for echo and conditions.
             Syncfusion args also contain selectedItem/previousItem (DOM objects) — intentionally omitted
             as they would serialize as [object Object].

```csharp
// Properties
IsSwiped { get; }  // Whether the selection was triggered by a swipe gesture.
PreviousIndex { get; }  // The zero-based index of the previously selected tab.
SelectedIndex { get; }  // The zero-based index of the newly selected tab.
```

### FusionTextArea

A FusionTextArea for long-form text entry.

```csharp
// Properties
ValueMember { get; }
```

### FusionTextAreaBlurArgs

Event payload delivered when a `FusionTextArea` loses focus.

```csharp
// Properties
Value { get; }  // Current text value.
```

### FusionTextAreaChangeArgs

Event payload delivered when a `FusionTextArea` value changes and focus leaves the input.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
PreviousValue { get; }  // Previous committed text value.
Value { get; }  // Current text value.
```

### FusionTextAreaEvents

Typed event descriptors for the `FusionTextArea` component.

```csharp
// Properties
Blur { get; }  // Fires when the component loses focus.
Changed { get; }  // Fires when the committed text changes after focus leaves the input.
Focus { get; }  // Fires when the component receives focus.
Input { get; }  // Fires as the text changes while editing.
```

### FusionTextAreaExtensions

Typed mutations and value reading for `FusionTextArea` in a reactive pipeline.

```csharp
FocusIn<T>()
FocusOut<T>()
SetValue<T>(value)
Value<T>()
```

### FusionTextAreaFocusArgs

Event payload delivered when a `FusionTextArea` receives focus.

```csharp
// Properties
Value { get; }  // Current text value.
```

### FusionTextAreaHtmlExtensions

Adds rendering helpers for `TextAreaBuilder})`.

```csharp
FusionTextArea<T>()
```

### FusionTextAreaInputArgs

Event payload delivered when a `FusionTextArea` value changes during input.

```csharp
// Properties
PreviousValue { get; }  // Previous input text value.
Value { get; }  // Current text value.
```

### FusionTextAreaReactiveExtensions

Wires `FusionTextArea` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionTextBox

A FusionTextBox for short text entry.

```csharp
// Properties
ValueMember { get; }
```

### FusionTextBoxBlurArgs

Event payload delivered when a `FusionTextBox` loses focus.

```csharp
// Properties
Value { get; }  // Current text value.
```

### FusionTextBoxChangeArgs

Event payload delivered when a `FusionTextBox` value changes and focus leaves the input.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
PreviousValue { get; }  // Previous committed text value.
Value { get; }  // Current text value.
```

### FusionTextBoxEvents

Typed event descriptors for the `FusionTextBox` component.

```csharp
// Properties
Blur { get; }  // Fires when the component loses focus.
Changed { get; }  // Fires when the committed text changes after focus leaves the input.
Focus { get; }  // Fires when the component receives focus.
Input { get; }  // Fires as the text changes while editing.
```

### FusionTextBoxExtensions

Typed mutations and value reading for `FusionTextBox` in a reactive pipeline.

```csharp
AddAppendIcon<T>(iconCssClass)
FocusIn<T>()
FocusOut<T>()
SetValue<T>(value)
Value<T>()
```

### FusionTextBoxFocusArgs

Event payload delivered when a `FusionTextBox` receives focus.

```csharp
// Properties
Value { get; }  // Current text value.
```

### FusionTextBoxHtmlExtensions

Adds rendering helpers for `TextBoxBuilder})`.

```csharp
FusionTextBox<T>()
```

### FusionTextBoxInputArgs

Event payload delivered when a `FusionTextBox` value changes during input.

```csharp
// Properties
PreviousValue { get; }  // Previous input text value.
Value { get; }  // Current text value.
```

### FusionTextBoxReactiveExtensions

Wires `FusionTextBox` events into the Reactive Plan.

```csharp
Reactive<T>()
```

### FusionTimePicker

A FusionTimePicker for selecting a time value.

```csharp
// Properties
ValueMember { get; }
```

### FusionTimePickerChangeArgs

Event payload delivered when a `FusionTimePicker` time changes.

```csharp
// Properties
IsInteracted { get; }  // Whether user interaction triggered the change.
Value { get; }  // New time value.
```

### FusionTimePickerEvents

Typed event descriptors for the `FusionTimePicker` component.

```csharp
// Properties
Changed { get; }  // Fires when the time value changes.
```

### FusionTimePickerExtensions

Typed mutations and value reading for `FusionTimePicker` in a reactive pipeline.

```csharp
FocusIn<T>()
FocusOut<T>()
SetValue<T>(value)
Value<T>()
```

### FusionTimePickerHtmlExtensions

Creates a FusionTimePicker inside a field wrapper, bound to a model property.

```csharp
FusionTimePicker<T>(setup, build)
```

### FusionTimePickerReactiveExtensions

Wires `FusionTimePicker` events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### FusionToolbar

Represents a Syncfusion Toolbar command component.
            Exposes disable/enable behavior and item click events; it is not an input component.

### FusionToolbarBuilder<T>

Wraps ToolbarBuilder.Render() output and carries plan metadata for reactive chaining.

### FusionToolbarClickedArgs

Event payload delivered when a Toolbar item is clicked.

```csharp
// Properties
Item { get; }  // Toolbar item metadata from the Syncfusion click event.
```

### FusionToolbarEvents

Typed event descriptors for the `FusionToolbar` component.

```csharp
// Properties
Clicked { get; }  // Fires when a toolbar item is clicked.
```

### FusionToolbarExtensions

Reactive pipeline extensions for `FusionToolbar`.

```csharp
Disable<T>()
```

### FusionToolbarHtmlExtensions

Creates typed `FusionToolbarBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionToolbar<T>(html, plan, elementId, build)
```

### FusionToolbarItem

Narrowed toolbar item payload proven from Syncfusion toolbar click behavior.

```csharp
// Properties
Disabled { get; }  // Whether the clicked item is disabled.
Id { get; }  // Toolbar item id.
Text { get; }  // Toolbar item text.
```

### FusionToolbarReactiveExtensions

Wires `FusionToolbar` events into the Reactive Plan.

### FusionTooltip

Represents a Syncfusion Tooltip display component.
            Exposes open/close/refresh methods and lifecycle events; it is not an input component.

### FusionTooltipBeforeCloseArgs

Payload for FusionTooltip.BeforeClose.
            Fires before the tooltip closes. Set cancel to true to prevent closing.

```csharp
// Properties
Cancel { get; }  // Set to true to prevent the tooltip from closing.
```

### FusionTooltipBeforeOpenArgs

Payload for FusionTooltip.BeforeOpen.
            Fires before the tooltip opens. Set cancel to true to prevent opening.

```csharp
// Properties
Cancel { get; }  // Set to true to prevent the tooltip from opening.
```

### FusionTooltipBeforeRenderArgs

Payload for FusionTooltip.BeforeRender.
            Fires before tooltip content renders. Used for dynamic content injection.
            Set cancel to true to prevent rendering.

```csharp
// Properties
Cancel { get; }  // Set to true to prevent the tooltip from rendering.
```

### FusionTooltipBuilder<T>

Wraps rendered Syncfusion tooltip markup while carrying plan metadata for `.Reactive(...)` chaining.

### FusionTooltipClosedArgs

Payload for FusionTooltip.Closed.
            Fires after the tooltip is hidden. Notification only — no actionable fields.

### FusionTooltipEvents

Typed event descriptors for the `FusionTooltip` component.

```csharp
// Properties
BeforeClose { get; }  // Fires before the tooltip closes. Set cancel to prevent closing.
BeforeOpen { get; }  // Fires before the tooltip opens. Set cancel to prevent opening.
BeforeRender { get; }  // Fires before tooltip content renders. Used for dynamic content.
Closed { get; }  // Fires after the tooltip is hidden.
Opened { get; }  // Fires after the tooltip is visible.
```

### FusionTooltipExtensions

Mutation extensions for `FusionTooltip` in a reactive pipeline.

```csharp
Close<T>()
Open<T>()
Refresh<T>()
```

### FusionTooltipHtmlExtensions

Creates typed `FusionTooltipBuilder<T>` instances for Reactive Plan wiring.

```csharp
FusionTooltip<T>(html, plan, elementId, build)
```

### FusionTooltipOpenedArgs

Payload for FusionTooltip.Opened.
            Fires after the tooltip becomes visible. Notification only — no actionable fields.

### FusionTooltipReactiveExtensions

Wires `FusionTooltip` events into the Reactive Plan.

```csharp
Reactive<T>(builder, eventSelector, pipeline)
```

---

## Fusion — Templates

### FusionConditionalBuilder<T>

Builds the content emitted inside a Syncfusion template conditional block.

```csharp
Badge()
Badge<T>()
Button()
Button()
Div()
EventButton<T>()
EventButton<T>()
Icon()
Icon()
Img<T>()
Img<T>()
Raw()
Render()
Span()
Span()
Span<T>()
Span<T>()
```

### FusionTemplate

Creates typed Syncfusion template HTML builders.

```csharp
Create<T>()
```

### FusionTemplateBuilder<T>

Builds a Syncfusion template HTML string with typed property bindings.

```csharp
Attr()
Badge()
Badge<T>()
Button()
Button()
ButtonFor<T>()
ButtonFor<T>()
Class()
Div()
EventButton<T>()
EventButton<T>()
Icon()
Icon()
Id()
Img<T>()
Img<T>()
Img<T>()
Link<T>()
Link<T>()
Raw()
Render()
ShowIf()
Span()
Span()
Span<T>()
Span<T>()
Text()
Text<T>()
ToString()
When()
When()
```

### FusionTemplateExpression

Converts supported C# expressions to Syncfusion template syntax.

```csharp
ToBinding<T>(expression)
ToCondition<T>(predicate)
ToPropertyPath<T>(expression)
```

### TemplateElements

Internal element rendering shared by root and conditional Syncfusion template builders.

```csharp
Button()
EventButton()
```

---

## Native

### NativeComponent

Base class for framework-owned native HTML components such as text inputs and checkboxes.

```csharp
// Properties
Vendor { get; }
```

---

## Native — AppLevel

### DrawerSize

Controls the width of the `NativeDrawer` panel.

### NativeDrawer

App-level slide-out drawer panel.

```csharp
// Properties
DefaultId { get; }
```

### NativeDrawerExtensions

Pipeline and layout extensions for the `IHtmlHelper)`.

```csharp
Close<T>()
NativeDrawer()
Open<T>()
SetSize<T>(self, size)
```

### NativeLoader

App-level loading overlay that covers its target container or the viewport.

```csharp
// Properties
DefaultId { get; }
```

### NativeLoaderExtensions

Pipeline and layout extensions for the `IHtmlHelper)`.

```csharp
Hide<T>()
NativeLoader()
SetTarget<T>(self, targetId)
SetTimeout<T>(self, ms)
Show<T>()
```

---

## Native — Components

### NativeActionLinkBuilder<T>

Builds the anchor element returned by `NativeActionLink`.

```csharp
Attr(name, value)
CssClass(css)
WriteTo()
```

### NativeActionLinkHtmlExtensions

HTML helper extensions for links that execute reactive request pipelines.

```csharp
NativeActionLink<T>(html, linkText, url, pipeline)
```

### NativeButton

Explicit-ID native HTML `<button>` component.

### NativeButtonBuilder<T>

Configures and renders a native HTML `<button>` element with an explicit ID.

```csharp
CssClass(css)
Type(type)
WriteTo()
```

### NativeButtonClickArgs

Event args for `Click`.

### NativeButtonEvents

Typed event descriptors for `NativeButton`.

```csharp
// Properties
Click { get; }  // Fires when the user clicks the button.
```

### NativeButtonExtensions

Reactive Plan operations for `NativeButton` text content and focus.

```csharp
FocusIn<T>()
SetText<T>(self, text)
```

### NativeButtonHtmlExtensions

Factory extension for rendering explicit-ID native buttons directly in a Razor view.

```csharp
NativeButton<T>(html, elementId, text)
```

### NativeButtonReactiveExtensions

Adds Reactive Plan event wiring to rendered `NativeButton` builders.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeCheckBox

A native HTML checkbox (`<input type="checkbox">`).

```csharp
// Properties
ValueMember { get; }
```

### NativeCheckBoxBuilder<T>

Configures and renders a native HTML `<input type="checkbox">` bound to a model property.

```csharp
CssClass(css)
WriteTo()
```

### NativeCheckBoxChangeArgs

Event args for `Changed`.

```csharp
// Properties
Checked { get; }  // Checked state after the change event.
```

### NativeCheckBoxEvents

Typed event descriptors for `NativeCheckBox`.

```csharp
// Properties
Changed { get; }  // Fires when the user checks or unchecks the checkbox.
```

### NativeCheckBoxExtensions

Reactive Plan operations for `NativeCheckBox` checked state and focus.

```csharp
FocusIn<T>()
SetChecked<T>(self, isChecked)
Value<T>()
```

### NativeCheckBoxHtmlExtensions

`InputField` extension for choosing `Boolean}})` as the wrapper's rendered control.

```csharp
NativeCheckBox<T>(setup, build)
```

### NativeCheckBoxReactiveExtensions

Wires `NativeCheckBox` DOM events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeCheckList

A native HTML checkbox list for multi-select scenarios.

```csharp
// Properties
ValueMember { get; }
```

### NativeCheckListBuilder<T>

Configures and renders a native HTML checkbox list bound to a model property.

```csharp
CssClass(css)
Items(items)
Option(value)
Option(value, text)
Option(value, text, description)
OptionCssClass(css)
WriteTo()
```

### NativeCheckListChangeArgs

Event args for `Changed`.

```csharp
// Properties
Value { get; }  // Checked values after the change event.
```

### NativeCheckListEvents

Typed event descriptors for `NativeCheckList`.

```csharp
// Properties
Changed { get; }  // Fires when the user checks or unchecks any checkbox in the list.
```

### NativeCheckListExtensions

Reactive Plan operations for `NativeCheckList` checked values and focus.

```csharp
FocusIn<T>()
SetValue<T>(self, value)
SetValue<T>(self, source, path)
Value<T>()
```

### NativeCheckListHtmlExtensions

`InputField` extension for choosing `NativeCheckListBuilder{<T>` as the wrapper's rendered control.

```csharp
NativeCheckList<T>(setup, build)
```

### NativeCheckListReactiveExtensions

Wires `NativeCheckList` DOM events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeDropDown

A native HTML dropdown (`<select>`).

```csharp
// Properties
ValueMember { get; }
```

### NativeDropDownBuilder<T>

Configures and renders a native HTML `<select>` element bound to a model property.

```csharp
CssClass(css)
Enabled(enabled)
Items(items)
Placeholder(optionLabel)
WriteTo()
```

### NativeDropDownChangeArgs

Event args for `Changed`.

```csharp
// Properties
Value { get; }  // Selected option value after the change event.
```

### NativeDropDownEvents

Typed event descriptors for `NativeDropDown`.

```csharp
// Properties
Changed { get; }  // Fires when the user selects a different option.
```

### NativeDropDownExtensions

Reactive Plan operations for `NativeDropDown` selected values and focus.

```csharp
FocusIn<T>()
SetValue<T>(self, value)
Value<T>()
```

### NativeDropDownHtmlExtensions

`InputField` extension for choosing `NativeDropDownBuilder{<T>` as the wrapper's rendered control.

```csharp
NativeDropDown<T>(setup, build)
```

### NativeDropDownReactiveExtensions

Wires `NativeDropDown` DOM events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeHiddenField

Reactive Plan-registered native HTML `<input type="hidden">` component.

```csharp
// Properties
ValueMember { get; }
```

### NativeHiddenFieldBuilder<T>

Renders a native HTML `<input type="hidden">` bound to a model property.

```csharp
WriteTo()
```

### NativeHiddenFieldChangeArgs

Event args for `Changed`.

```csharp
// Properties
Value { get; }  // Hidden input value after the change event.
```

### NativeHiddenFieldEvents

Typed event descriptors for `NativeHiddenField`.

```csharp
// Properties
Changed { get; }  // Fires when a DOM `change` event is dispatched for the hidden input.
```

### NativeHiddenFieldExtensions

Reactive Plan operations for `NativeHiddenField` values.

```csharp
SetValue<T>(self, value)
SetValue<T>(self, source)
SetValue<T>(self, source, path)
Value<T>()
```

### NativeHiddenFieldHtmlExtensions

Factory extension for rendering model-bound hidden fields directly in a Razor view.

```csharp
HiddenFieldFor<T>(html, plan, expression)
```

### NativeHiddenFieldReactiveExtensions

Wires `NativeHiddenField` DOM events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeRadioGroup

A native HTML radio button group.

```csharp
// Properties
ValueMember { get; }
```

### NativeRadioGroupBuilder<T>

Configures and renders a native HTML radio button group bound to a model property.

```csharp
CssClass(css)
Items(items)
Option(value)
Option(value, text)
Option(value, text, description)
OptionCssClass(css)
WriteTo()
```

### NativeRadioGroupChangeArgs

Event args for `Changed`.

```csharp
// Properties
Value { get; }  // Selected radio button value after the change event.
```

### NativeRadioGroupEvents

Typed event descriptors for `NativeRadioGroup`.

```csharp
// Properties
Changed { get; }  // Fires when the user selects a different radio option.
```

### NativeRadioGroupExtensions

Reactive Plan operations for `NativeRadioGroup` selected values and focus.

```csharp
FocusIn<T>()
SetValue<T>(self, value)
SetValue<T>(self, source, path)
Value<T>()
```

### NativeRadioGroupHtmlExtensions

`InputField` extension for choosing `NativeRadioGroupBuilder{<T>` as the wrapper's rendered control.

```csharp
NativeRadioGroup<T>(setup, build)
```

### NativeRadioGroupReactiveExtensions

Adds Reactive Plan event wiring to rendered `NativeRadioGroup` builders.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeTextArea

A native HTML multi-line text input (`<textarea>`).

```csharp
// Properties
ValueMember { get; }
```

### NativeTextAreaBuilder<T>

Configures and renders a native HTML `<textarea>` element bound to a model property.

```csharp
CssClass(css)
Placeholder(placeholder)
Rows(rows)
WriteTo()
```

### NativeTextAreaChangeArgs

Event args for `Changed`.

```csharp
// Properties
Value { get; }  // Textarea value captured from the change event.
```

### NativeTextAreaEvents

Typed event descriptors for `NativeTextArea`.

```csharp
// Properties
Changed { get; }  // Fires when the user changes the textarea value and leaves the field.
```

### NativeTextAreaExtensions

Reactive Plan operations for `NativeTextArea` values and focus.

```csharp
FocusIn<T>()
SetValue<T>(self, value)
Value<T>()
```

### NativeTextAreaHtmlExtensions

`InputField` extension for choosing `NativeTextAreaBuilder{<T>` as the wrapper's rendered control.

```csharp
NativeTextArea<T>(setup, build)
```

### NativeTextAreaReactiveExtensions

Wires `NativeTextArea` DOM events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### NativeTextBox

A native HTML text input (`<input type="text">`).

```csharp
// Properties
ValueMember { get; }
```

### NativeTextBoxBuilder<T>

Configures and renders a native HTML `<input>` element bound to a model property.

```csharp
CssClass(css)
Placeholder(placeholder)
Type(type)
WriteTo()
```

### NativeTextBoxChangeArgs

Event args for `Changed`.

```csharp
// Properties
Value { get; }  // Text input value captured from the change event.
```

### NativeTextBoxEvents

Typed event descriptors for `NativeTextBox`.

```csharp
// Properties
Changed { get; }  // Fires when the user changes the input value and leaves the field.
```

### NativeTextBoxExtensions

Reactive Plan operations for `NativeTextBox` values and focus.

```csharp
FocusIn<T>()
SetValue<T>(self, value)
Value<T>()
```

### NativeTextBoxHtmlExtensions

`InputField` extension for choosing `NativeTextBoxBuilder{<T>` as the wrapper's rendered control.

```csharp
NativeTextBox<T>(setup, build)
```

### NativeTextBoxReactiveExtensions

Wires `NativeTextBox` DOM events into the Reactive Plan.

```csharp
Reactive<T>(builder, plan, eventSelector, pipeline)
```

### RadioButtonItem

A single radio or checkbox option with a value, display text, and optional description.

```csharp
// Properties
Description { get; }  // Optional secondary description shown below the display text.
Text { get; }  // Display text shown next to the radio button or checkbox.
Value { get; }  // Option value written to form posts and component reads.
```

---

## Native — Extensions

### NativeGatherExtensions

Gather shorthand that defaults to `NativeTextBox` when no component
            type is specified.

```csharp
Include<T>(self, expr)
```

---
